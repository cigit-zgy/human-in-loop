# HUMAN_IN_LOOP_RELEASE_V012_14

## Task type

FORMAL RELEASE TASK

## Repository

`cigit-zgy/human-in-loop`

## Release branch

`task/260909-release-v0.1.2`

## Accepted candidate

Candidate/report branch head:

`d08215ef83b45259e27c516bf07407cd979913aa`

Implementation qualification head:

`82baf42feddcad5da409426a0ecaaa1d0bce6972`

ChatGPT acceptance: **PASS**.

## Standing User authorization

The User has explicitly authorized routine release publication without another confirmation after ChatGPT acceptance. This authorization is now recorded in `AGENTS.md` on this release branch.

Do **not** call `ask_human` merely to reconfirm merge, tag, `v0.1.2`, GitHub Release creation, machine-wide Skill pin, or normal release integration.

Only stop for a genuinely new material decision involving security/privacy boundary expansion, licensing/repository visibility, destructive history/state change, new external service/credential scope, or another public effect materially outside the already accepted release plan.

## Pinned collaboration authority

`cigit-zgy/agent-collaboration@8601466216515125bf8b17893b2a8e8673bab79e`

Fresh-fetch first; inspect branch/HEAD/upstream/worktrees; preserve User state; all task scratch under `<PROJECT_ROOT>/tmp/HUMAN_IN_LOOP_RELEASE_V012_14/`; no reset/force-push/destructive history rewrite; commit/push/fresh-fetch proof; durable Codex report.

## Release scope

Publish exactly `v0.1.2` as the accepted rich-decision-body compatibility release.

This release adds only:

```text
ask_human.detail
= optional bounded multiline substantive decision evidence

context
= existing compact metadata path

configurable iMessage decision budgets
= default detail 1000 Unicode chars
= default complete confirmation 1500 Unicode chars
= absolute ceilings 4500 / 5000

redacted payload failure reasons
= detail_too_long
= rendered_text_too_long
= invalid_decision_budget
```

Do not broaden scope.

## Non-regression boundary

The following are already working and are not redesign targets:

- exactly two maintained remote channels: Feishu and iMessage;
- exactly two public MCP tools: `ask_human`, `notify_human`;
- dedicated Bot macOS user / distinct Bot Apple Account topology;
- explicit iMessage-only send and no-SMS fallback;
- Messages DB / worker IPC / send/watch/correlation semantics;
- stable signing, TCC, bootstrap and macOS setup lifecycle;
- Feishu authentication/router behavior;
- daemon lifecycle;
- `notify_human` semantics and notification budget;
- v0.1.1 canonical-HOME migration;
- checkpoint classifier and terminal-report semantics;
- unrelated UI, permission, YOLO, agent-lifecycle features.

Do not modify these except for a mechanically necessary version/readme/release projection. If release verification exposes a regression, STOP rather than redesigning unrelated subsystems in this task.

## Required work

### 1. State and ancestry

- Fresh-fetch `master`, candidate branch, release branch, tags.
- Verify candidate ancestry from current production master `357a43762ef2fd334de47eb1869e696102012c52` and that no material remote master divergence appeared after task issue.
- Preserve unrelated User worktrees/configuration.
- Verify `v0.1.2` tag/release does not already exist with conflicting target/state.

### 2. Release-content audit

Confirm on the exact release candidate:

- all maintained version-bearing manifests = `0.1.2`;
- `ask_human.detail` is optional/backward-compatible;
- multiline body preserves internal line breaks and meaningful blank lines;
- old `context` semantics remain compact metadata;
- default budgets = `1000` detail / `1500` complete message;
- absolute ceilings = `4500` / `5000`;
- configuration values fail closed when zero/inverted/over-ceiling;
- no silent truncation;
- diagnostics remain fixed/redacted and contain no body/private transport data;
- old callers without `detail` still work;
- `notify_human` behavior is unchanged;
- no private WME article text or User transport identity is tracked.

### 3. Minimal release documentation

Update README only as needed to state current release `v0.1.2` and summarize:

- multiline `ask_human.detail` for substantive decision evidence;
- compact `context` remains metadata;
- default/configurable/absolute bounded limits;
- no silent truncation;
- transport/security topology unchanged.

Use existing `docs/release-notes/v0.1.2.md` as the release-note authority; correct only inaccuracies discovered during release audit.

### 4. Integrate safely

Fresh-fetch immediately before integration.

Integrate the accepted release line into `master` using normal non-destructive Git history. Prefer fast-forward where valid; otherwise use an ordinary merge preserving ancestry. Never reset/rebase/force-push master.

Before live qualification prove:

```text
local master HEAD == origin/master == fresh FETCH_HEAD
```

### 5. Full release regression

On the integrated release tree run material release gates:

- version consistency;
- `cargo fmt --check`;
- `cargo clippy --all-targets -- -D warnings`;
- full Rust tests;
- frontend/Node tests;
- production build;
- relevant MCP/config/iMessage/Feishu/notification focused tests;
- security/privacy/dependency checks appropriate to changed files;
- no new ignored-test count without explicit blocker classification.

### 6. Install/update smoke

Install the integrated `v0.1.2` candidate using the existing stable production signing/update path.

Do not reinstall TCC, recreate Bot user, change Messages/Apple Account, or create another runtime HOME.

Verify:

```text
installed human-in-loop = 0.1.2
iMessage health = ready
macOS setup = COMPLETE
MCP tools = exactly ask_human + notify_human
canonical config remains shared
no managed legacy HUMAN_IN_LOOP_HOME
```

### 7. Exactly one real WME-sized rich-body qualification

This is the blocking release gate.

From the **actual installed Codex MCP registration**, execute exactly one harmless synthetic rich-body `ask_human` request that matches the real WME workload class without containing real article data.

Required payload characteristics:

- 600–800 Chinese Unicode characters in `detail`;
- multiline with the seven review-card-style sections used by qualification;
- short `question`;
- compact metadata `context` only if useful;
- five stable choices analogous in size/count to the WME review workflow;
- no `recommended_choice`.

Required real path:

```text
installed Codex MCP process
→ canonical config
→ ask_human.detail
→ canonical coordinator
→ dedicated Bot worker
→ explicit iMessage-only send
→ genuine iPhone notification
→ one human reply
→ exactly one correlated canonical selected_choice_id
```

Prove:

- body line breaks are preserved in the sent presentation sufficiently to retain section structure;
- exactly one application send mutation;
- actual service iMessage;
- exactly one canonical result;
- watcher/request/client cleanup;
- no retry if delivery/result becomes uncertain.

Do not send more than one real rich-body qualification request. If uncertain or failed, STOP release before tag/publication.

### 8. Machine-wide Skill pin

After the exact release commit is known and live qualification passes, update/verify the machine-wide Skill pin to the immutable `v0.1.2` release commit while preserving unrelated `$CODEX_HOME` state.

Verify installed Codex MCP registration remains canonical and contains no managed isolated HOME override.

### 9. Tag and public GitHub Release

Only after every blocking gate passes:

1. Create repository-standard annotated tag `v0.1.2` at the exact verified release commit.
2. Push tag normally; never force.
3. Create public GitHub Release `human-in-loop v0.1.2`.
4. `draft=false`, `prerelease=false`.
5. Use committed `docs/release-notes/v0.1.2.md` as release body.
6. Publish source-only; no unnotarized binary/app assets.
7. Verify peeled tag target, public Release state, default-branch README and source archives.

### 10. Terminal notification

After durable release truth and report synchronization exist, attempt exactly one compact `notify_human` terminal notification.

No acknowledgement wait. No retry after uncertain delivery. Notification failure does not rewrite an otherwise established release verdict.

### 11. Detailed report

Write:

`reports/codex/260909_codex_04.md`

If occupied, use the next free `260909_codex_XX.md` and explain.

Report at minimum:

```text
VERDICT
RELEASE_VERSION
RELEASE_COMMIT
MASTER_HEAD
TAG_TARGET
GITHUB_RELEASE
README_UPDATED
VERSION_CONSISTENCY
DETAIL_SCHEMA
MULTILINE_PRESERVATION
DEFAULT_LIMITS
USER_CONFIGURABLE_LIMITS
ABSOLUTE_SAFETY_CEILINGS
NO_SILENT_TRUNCATION
BACKWARD_COMPATIBILITY
NO_AVAILABLE_CHANNEL_DIAGNOSTICS
IMESSAGE_HEALTH
MACOS_SETUP
INSTALLED_MCP_TOOLS
CANONICAL_CONFIG_SHARED
REAL_WME_SIZED_ASK_HUMAN_QUALIFICATION
REAL_PAYLOAD_CHAR_COUNT
REAL_SEND_COUNT
REAL_RESULT_COUNT
MACHINE_SKILL_PIN
IMESSAGE_REGRESSION
FEISHU_REGRESSION
NOTIFY_HUMAN_REGRESSION
HOME_MIGRATION_REGRESSION
TEST_SUMMARY
SECURITY_SUMMARY
PRIVACY_SUMMARY
HUMAN_NOTIFICATION
WORKTREE_CLEANUP
FRESH_FETCH_PROOF
```

Also separate evidence into:

```text
PROVEN_LIVE
PROVEN_SYNTHETIC
INFERRED_ONLY
```

Do not claim WME production itself has been reviewed; this release qualification uses synthetic content only.

## STOP conditions

STOP before tag/release if any of these occurs:

- candidate/master ancestry cannot be safely reconciled;
- integration requires destructive history rewrite;
- real rich-body installed-MCP qualification fails/uncertain;
- iMessage health is not `ready` or setup not `COMPLETE`;
- MCP public tool count differs from exactly two;
- old callers regress;
- `notify_human`, Feishu, v0.1.1 HOME migration, Bot/TCC/correlation/no-SMS invariants regress;
- body is silently truncated;
- configured/absolute ceilings are bypassed;
- private body/recipient/chat/GUID/credential data leaks into logs/report/release;
- tests/build/security/privacy gates materially fail;
- conflicting existing `v0.1.2` tag/Release exists.

## Acceptance

PASS only when the accepted candidate is integrated without destructive rewrite, the installed v0.1.2 runtime passes full regression and one real WME-sized multiline iMessage round trip, machine-wide Skill pin is updated to the immutable release commit, `v0.1.2` tag and public non-prerelease GitHub Release are verified, and no existing transport/security behavior regresses.
