# HUMAN_IN_LOOP_HEADLESS_INBOUND_SYNC_20

## Task type

FORMAL RECOVERY / IMPLEMENTATION / LIVE QUALIFICATION TASK

## Repository

`cigit-zgy/human-in-loop`

## Task branch

`task/260910-headless-inbound-sync-v0.1.3`

## Baseline

Blocked v0.1.3 qualification report commit:

`b8f9dba735dae95772bed2e159c98d6312c4072e`

The inherited implementation already removed Feishu, retains iMessage-only transport, decimal `TOKEN-OPTION`, rich multiline detail, default-zero progress notifications, at-most-one terminal notification, quoted no-large-preview report locator, simplified onboarding, optional Bot automatic login, stable signing/TCC, and canonical HOME behavior.

Do not discard those accepted/qualified changes.

## Pinned collaboration authority

`cigit-zgy/agent-collaboration@8601466216515125bf8b17893b2a8e8673bab79e`

Follow it exactly: fresh-fetch first; inspect branch/HEAD/upstream/worktrees; preserve unrelated User state; task scratch only under `<PROJECT_ROOT>/tmp/HUMAN_IN_LOOP_HEADLESS_INBOUND_SYNC_20/`; no reset/force-push/destructive history rewrite; commit/push/fresh-fetch proof; durable Codex report.

## User decisions already resolved

The User explicitly decided:

1. The maintained product is iMessage-only.
2. The persistent `AskHuman - running` menu-bar/tray GUI is not wanted and may be removed because the accepted production architecture is headless.
3. The old AskHuman GUI/updater must not offer upstream AskHuman versions (for example `0.13.x`) as human-in-loop updates.
4. Keep the decimal correlation reply grammar such as `48273-1`; do not replace it with bare digits.
5. Preserve all previously qualified transport/security boundaries.
6. Routine release publication after ChatGPT acceptance remains pre-authorized; this task itself does not publish.

No additional `ask_human` is required to reconfirm these decisions.

## Living design already frozen on this task branch

ChatGPT has updated/added:

- `design/00_overview.md`
- `design/11_imessage_inbound_sync_recovery.md`
- `design/README.md`

Treat these as authority.

## Incident A — legacy visible GUI host

The User currently sees a persistent macOS menu-bar item similar to:

```text
AskHuman - running
Version 0.1.2
Update available (0.13.1)
Codex integration needs an update
Settings
History
Todos
New Agent Task
Check for Updates
Restart Daemon
Stop Daemon
```

Current source still contains legacy menu-bar/tray configuration with an `Always` default. Current product design, however, defines the maintained production runtime as headless.

This task must identify the exact currently running process/bundle/login-item/launchd ownership before changing anything. Do not infer a path or delete arbitrary applications.

## Incident B — delivered reply not synchronized to Bot Messages

The previous real qualification proved:

```text
one installed-MCP ask_human request sent
→ phone received one message
→ decimal five-digit token rendered
→ exact TOKEN-OPTION reply instruction rendered
→ quoted report locator had no large Rich Link Preview
→ locator was tappable and complete
→ User sent exact TOKEN-OPTION reply
→ Apple showed Delivered
→ request remained pending >15 minutes
→ no new Bot Messages row reached the watcher
→ no canonical selected_choice_id/source_channel_id
→ request cancelled and cleaned
```

The former Feishu/Keychain startup blocker is gone. Current unresolved boundary is Bot Messages inbound synchronization / watcher visibility only.

## Scope A — remove the legacy AskHuman tray/GUI production host

### Required inventory first

Before mutation, record redacted evidence for:

- exact menu-bar process executable path and code/bundle identity;
- owning user;
- launch/login item or launchd source that starts it;
- whether it is the same executable/process required by MCP, the headless daemon, or the Bot worker;
- updater authority/source responsible for the `0.13.1` offer, if determinable from product-owned code/config without exposing private data.

### Required outcome

Remove the legacy GUI host from the maintained production path so that normal human-in-loop operation does not auto-launch or require a visible menu-bar application.

At minimum eliminate production behavior/surfaces owned solely by this legacy host:

```text
persistent AskHuman menu-bar/tray item
legacy GUI auto/login launch wiring
legacy AskHuman upstream update notification/check from human-in-loop production
GUI daemon-control dependency when the headless daemon already owns lifecycle
```

Remove dead `MenuBarIconMode`/tray/updater configuration or frontend/runtime code when it is exclusively owned by the retired production GUI and safe to remove. Do not broaden into an unrelated frontend rewrite solely for code cleanliness.

It is acceptable to retain historical source/provenance only where needed for license/audit history, but no current installed production component may start the legacy GUI/updater.

### Must preserve

Do NOT remove or disable:

- `human-in-loop` headless daemon/coordinator;
- Codex MCP registration;
- `ask_human` / `notify_human` public tools;
- dedicated Bot-user worker/LaunchAgent;
- iMessage transport and `imsg`;
- stable signing/TCC identity;
- setup/recovery CLI surfaces needed for headless operation;
- user data/history unless the removed legacy host owns a separate disposable cache proven unnecessary.

Do not delete arbitrary `/Applications` content or unrelated login items. Only remove exact product-owned legacy state after positive identification.

## Scope B — diagnose the old Delivered reply before sending anything new

Do not immediately issue another real test request.

First, using only the authorized Bot-worker/public-basic `imsg` boundary, determine whether the previously Delivered reply has since become visible in Bot-owned chat history.

Classify exactly one of:

```text
HISTORY_ROW_PRESENT_WATCH_MISSED
HISTORY_ROW_STILL_ABSENT
INSUFFICIENT_EVIDENCE
```

The prior request is cancelled. Even if its row later appears, it MUST remain late/stale and MUST NOT mutate or resurrect a canonical result.

Do not expose the real token, recipient, chat id/GUID, row id, or raw private message in the report.

## Scope C — isolate watcher gap versus Messages synchronization gap

Use the diagnostic order in `design/11_imessage_inbound_sync_recovery.md`.

Required evidence should distinguish:

### Case 1: inbound row exists in history but watcher missed it

Then treat this as a product watcher/reconciliation defect. Implement the minimum safe recovery needed so that post-send inbound rows cannot be silently missed by the live watch path. A bounded history reconciliation/catch-up path is acceptable only when it applies the exact original correlation predicates and safe cursor/send boundary.

It must not accept ambiguous old rows, broaden correlation, or poll unboundedly.

### Case 2: inbound row is absent from Bot Messages storage

Then diagnose the Bot Messages session/synchronization path using public/reversible macOS and `imsg` surfaces only. Determine whether the session is healthy enough for outbound but stale for inbound.

Allowed evidence-driven recovery includes:

- restart/relaunch exact Bot worker;
- restart/relaunch Messages.app or ordinary Bot-user messaging service when justified and possible through the existing user-session boundary;
- revalidate Bot Messages/iMessage identity without changing it;
- revalidate the existing direct chat;
- rebuild a watcher from a safe cursor/history boundary.

Do not sign out Apple Account, delete Messages data, reset TCC, recreate Bot user, change recipient/sender, disable FileVault/SIP, use private frameworks, inject IMCore, or bypass Bot-user privacy with sudo/direct primary-user `chat.db` access.

If a genuine native Bot-user Apple Account reauthentication is the only remaining recovery, return one precise `USER_CHECKPOINT` and stop; do not normalize repeated manual intervention.

## Scope D — improve health/diagnostics if justified

If current health can report `ready` while a reproducible inbound synchronization stall exists, add the minimum owning diagnostic state equivalent to:

`BOT_MESSAGES_INBOUND_SYNC_STALLED`

Do not mark the channel healthy merely because outbound sending works.

Avoid speculative always-on probing that sends messages. Health/preflight should remain non-mutating unless a controlled qualification is explicitly underway.

## Scope E — one fresh real qualification only after recovery is proven

Only after:

- legacy tray host removal does not break daemon/MCP/worker;
- old request is terminally cancelled;
- no stale watcher/client remains;
- the old Delivered reply has been classified;
- inbound synchronization/watcher recovery is technically justified and health is ready;

may the task send exactly one new harmless installed-MCP `ask_human` qualification request.

Required real path:

```text
installed Codex MCP
→ canonical config
→ ask_human
→ decimal five-digit TOKEN-OPTION presentation
→ explicit iMessage-only send
→ phone reply using exact TOKEN-OPTION
→ one Bot-owned post-send inbound row
→ strict correlation
→ exactly one canonical MCP result
→ selected_choice_id proven
→ source_channel_id = imessage
→ complete watcher/request/client cleanup
```

Use synthetic/non-production content. Do not mutate WME/scientific/gold state.

Do not resend if delivery/result becomes uncertain. Exactly one fresh qualification request is the default budget for this task.

The previously qualified quoted report locator presentation does not need another dedicated link probe unless it naturally appears in this one combined qualification.

## Regression requirements

At minimum prove:

```text
maintained remote channels exactly [imessage]
Feishu runtime/config/keychain remains absent
legacy AskHuman tray/menu-bar host no longer auto-runs
legacy AskHuman updater cannot offer 0.13.x to human-in-loop
headless daemon starts unattended
MCP tools exactly ask_human + notify_human
Bot worker ready
iMessage explicit service + no-SMS invariant
numeric TOKEN-OPTION parser/render/correlation unchanged
rich multiline detail unchanged
terminal progress default 0
one terminal task <= one notify_human attempt
quoted report locator remains same-message and no-large-preview compatible
optional Bot autologin regression
canonical HOME regression
cancellation/duplicate/late/race cleanup
```

Run focused tests first, then the full material regression/build/security/privacy gates appropriate to the changed surfaces.

## Security/privacy requirements

No report/test artifact may contain:

- private recipient/sender handle;
- Apple Account identifier;
- real TOKEN;
- chat id/GUID/row id;
- raw Messages content;
- credentials;
- Keychain contents.

Do not introduce a generic command/file/database capability through MCP or the Bot worker.

## Reporting

Write:

`reports/codex/260910_codex_04.md`

If occupied, use the next free `260910_codex_XX.md` and explain.

Report at minimum:

```text
VERDICT
TASK_SOURCE
INHERITED_BLOCKED_REPORT
LEGACY_TRAY_PROCESS_IDENTIFIED
LEGACY_TRAY_REQUIRED_FOR_RUNTIME
LEGACY_TRAY_REMOVED
LEGACY_GUI_AUTOSTART_REMOVED
LEGACY_UPDATER_REMOVED
ASKHUMAN_013X_UPDATE_PATH_REMOVED
HEADLESS_DAEMON_STARTUP
MCP_SURFACE
BOT_WORKER_HEALTH
IMESSAGE_HEALTH
OLD_DELIVERED_REPLY_CLASSIFICATION
ROOT_CAUSE_CLASSIFICATION
WATCHER_RECONCILIATION_CHANGED
MESSAGES_SESSION_RECOVERY_CHANGED
INBOUND_SYNC_DIAGNOSTIC_STATE
REAL_ASK_HUMAN_QUALIFICATION
REAL_SEND_COUNT
REAL_RESULT_COUNT
REAL_SELECTED_CHOICE_ID
REAL_SOURCE_CHANNEL_ID
NUMERIC_TOKEN_REGRESSION
LINK_PRESENTATION_REGRESSION
FEISHU_REMOVAL_REGRESSION
NO_SMS_REGRESSION
RICH_BODY_REGRESSION
AUTOLOGIN_REGRESSION
HOME_MIGRATION_REGRESSION
TERMINAL_NOTIFY_REGRESSION
TEST_SUMMARY
SECURITY_SUMMARY
PRIVACY_SUMMARY
WORKTREE_CLEANUP
FRESH_FETCH_PROOF
HUMAN_NOTIFICATION
```

Separate:

```text
PROVEN_LIVE
PROVEN_DETERMINISTIC
INFERRED_ONLY
```

Do not claim the round trip fixed unless a fresh real canonical result exists.

## STOP conditions

Stop and report `BLOCKED` if:

- the identified tray process is actually required by MCP/daemon/Bot worker and cannot be separated without reopening architecture;
- recovery would require Apple Account sign-out/reconfiguration, TCC reset, Bot-user recreation, private API, SIP/FileVault weakening, raw primary-user access to Bot `chat.db`, or unbounded retry;
- old reply evidence is ambiguous and cannot be safely classified;
- one fresh qualification is sent but no canonical result returns;
- existing iMessage/no-SMS/correlation/security invariants regress;
- unrelated User state would need destructive mutation.

## Completion

PASS only when the production installation is genuinely headless (no required/autostart legacy AskHuman tray/updater), the inbound synchronization/watcher root cause is resolved or boundedly recovered, and one fresh installed-MCP iMessage request completes with exactly one canonical correlated result and clean teardown while all previously qualified v0.1.3 behavior remains intact.
