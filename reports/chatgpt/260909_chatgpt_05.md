# HUMAN_IN_LOOP_RELEASE_V012_RECOVERY_15

FORMAL RELEASE RECOVERY TASK

Repository: `cigit-zgy/human-in-loop`

Standing User authorization: routine publication after ChatGPT acceptance is already authorized; do not ask again for merge/tag/release authorization.

Pinned collaboration authority: `cigit-zgy/agent-collaboration@8601466216515125bf8b17893b2a8e8673bab79e`.

Canonical v0.1.2 release code commit: `40381285c68d67f6e23ba4e9d637855923df2d4f`.

Prior report: `reports/codex/260909_codex_04.md`.

Accepted diagnosis: the blocked release was caused by a task-local verifier defect, not a proven product/transport regression. The verifier incorrectly required successful MCP responses to omit `isError`; with `rmcp 2.2.0`, success may serialize `isError:false`. It threw before persisting `structuredContent`, so the one live result was lost. No retry was allowed in that task. No tag/Release/Skill-pin mutation occurred.

Scope is recovery only. Do not modify production rich-body implementation, Bot/TCC/iMessage transport, Feishu, notify_human, HOME migration, checkpoint classifier, or unrelated UI/configuration unless new evidence disproves the accepted diagnosis; if so, STOP instead of patching opportunistically.

1. Fresh-fetch and prove `40381285...` is an ancestor of current master; classify later commits as report/notification-only unless evidence says otherwise. Verify no conflicting `v0.1.2` tag/Release exists, machine Skill pin remains v0.1.1, installed binary is v0.1.2, iMessage health `ready`, setup `COMPLETE`, MCP tools exactly `ask_human`,`notify_human`, canonical config shared, no legacy `HUMAN_IN_LOOP_HOME`.

2. Before any real send, validate corrected verifier parsing with synthetic MCP results: `isError` absent => success; `isError:false` => success; `isError:true` => failure. Ensure successful `structuredContent` is durably captured before any later assertion/teardown. Persist only redacted `request_id`, `selected_choice_id`, `source_channel_id`; never recipient/token/chat/GUID/row/message/body/credential/private address.

3. Then issue exactly one new real installed-Codex-MCP `ask_human` rich-body qualification: 600–800 Chinese Unicode chars in `detail`, seven synthetic review-card sections, short question, compact context, exactly five stable choices, no recommendation, no real WME article content. Required path: installed Codex MCP -> canonical config -> ask_human.detail -> coordinator -> Bot worker -> explicit iMessage-only send -> genuine iPhone notification -> one human reply -> one canonical result.

Blocking evidence: payload chars 600–800; send count=1; result count=1; selected_choice_id is one declared stable id; source_channel_id=`imessage`; request_id non-empty; no `NoAvailableChannel`; active requests after completion=0; watcher/client/process cleanup PASS. Do not retry uncertain delivery/result.

4. On PASS, publish directly without another user checkpoint: update machine-wide Skill pin to `cigit-zgy/human-in-loop@40381285c68d67f6e23ba4e9d637855923df2d4f:SKILL.md`; verify MCP registration has no legacy isolated HOME; create annotated tag `v0.1.2` at exactly `40381285...`; push normally; create public GitHub Release `human-in-loop v0.1.2`, `draft=false`, `prerelease=false`, using committed `docs/release-notes/v0.1.2.md`; source-only, no unnotarized assets. Report-only commits after release commit must not move tag.

5. Reuse prior full deterministic regression evidence unless source/install state changed materially. Re-check focused runtime gates: installed 0.1.2, iMessage ready, setup COMPLETE, exactly two MCP tools, canonical config shared, no legacy HOME, rich-body limits intact.

6. After durable publication/report truth, attempt exactly one compact terminal `notify_human`; no acknowledgement wait, no retry after uncertain delivery. Notification failure does not rewrite an established PASS.

7. Write `reports/codex/260909_codex_05.md` (or next free number, explain). Include: VERDICT, RECOVERY_CLASSIFICATION, RELEASE_VERSION, RELEASE_COMMIT, MASTER_HEAD, TAG_TARGET, GITHUB_RELEASE, VERIFIER_FALSE_ISERROR_TEST, VERIFIER_RESULT_CAPTURE_TEST, REAL_WME_SIZED_ASK_HUMAN_QUALIFICATION, REAL_PAYLOAD_CHAR_COUNT, REAL_SEND_COUNT, REAL_RESULT_COUNT, REAL_SELECTED_CHOICE_ID, REAL_SOURCE_CHANNEL_ID, NO_AVAILABLE_CHANNEL_OBSERVED, IMESSAGE_HEALTH, MACOS_SETUP, INSTALLED_MCP_TOOLS, CANONICAL_CONFIG_SHARED, MACHINE_SKILL_PIN, PRODUCTION_CODE_CHANGED, FOCUSED_REGRESSION, SECURITY_PRIVACY_BOUNDARY, HUMAN_NOTIFICATION, WORKTREE_CLEANUP, FRESH_FETCH_PROOF. Separate PROVEN_LIVE / PROVEN_REUSED / INFERRED_ONLY.

STOP without publication if corrected verifier still mishandles `isError:false`, canonical result cannot be captured, the one new live request fails/uncertain, NoAvailableChannel occurs, send/result counts or selected choice/channel/request id are not proven, release ancestry/install identity is ambiguous, runtime gates regress, destructive history rewrite would be required, or private data would leak.

PASS only when verifier defect is proven fixed, exactly one new live WME-sized rich-body request yields one durably captured canonical iMessage result, no product regression is found, machine Skill pin and `v0.1.2` tag both point to exact release commit `40381285c68d67f6e23ba4e9d637855923df2d4f`, public non-prerelease source-only GitHub Release exists, and final report is committed/pushed/fresh-fetched.