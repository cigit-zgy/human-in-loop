---
artifact_type: chatgpt_task
artifact_id: 260908_chatgpt_01
task_id: HUMAN_IN_LOOP_CODEX_HITL_06
title: Integrate human-in-loop as a machine-wide Codex decision and reporting layer
date: 2026-09-08
project: human-in-loop
repository: cigit-zgy/human-in-loop
status: issued
execution_mode: formal
summary: >
  Extend the validated human-in-loop MCP server with non-blocking notify_human,
  install the committed Skill as an exact pinned Codex discovery authority,
  add a thin machine-wide Codex AGENTS activation rule, register the MCP server
  in Codex config without disturbing existing settings, and qualify both blocking
  checkpoint and terminal notification behavior end to end on the User's Mac.
task_branch: task/260908-codex-hitl-global-integration
baseline_sha: 03d7b7cdb1663504c60e2ae4cbdfbf083080651b
verification_level: level_3
collaboration_commit: ad88170b23920ddac0bff9a2fd467aa0c59917cf
coding_skill_profile: >
  cigit-zgy/agent-collaboration@ad88170b23920ddac0bff9a2fd467aa0c59917cf:
  references/collaboration/shared-coding-skills.md
codex_report: reports/codex/260908_codex_01.md
post_acceptance_integration: USER_CHECKPOINT
---

# Integrate human-in-loop as a machine-wide Codex decision and reporting layer

## Mission

Implement the already accepted Codex integration contract without inventing new semantics.

The required final architecture is:

```text
Codex machine-wide policy
├── $CODEX_HOME/AGENTS.md
│   └── thin activation + fail-closed invariant only
├── supported Codex Skill discovery
│   └── exact pinned human-in-loop SKILL.md
└── $CODEX_HOME/config.toml
    └── production human-in-loop MCP server registration

Codex task lifecycle
├── mandatory decision boundary
│   → ask_human
│   → block until one correlated result
│   → continue/stop from stable selected choice
└── terminal task state
    → normal task result/report + repository synchronization first
    → notify_human
    → no acknowledgement wait
    → final Codex response
```

This task starts from the already Level-3-qualified `ask_human` implementation on `task/260907-mcp-interface`; do not reimplement the human-in-loop core.

## Authority and frozen semantics

Before mutation, fresh-fetch and verify the exact task branch/task coordinate under the pinned collaboration synchronization contract.

Read in this order:

```text
AGENTS.md
→ design/README.md
→ design/05_mcp_interface.md
→ design/06_codex_integration.md
→ SKILL.md
→ directly relevant MCP/coordinator/channel implementation/tests
→ this task
```

Historical rationale only when needed:

```text
reports/concept/260908_concept_01.md
reports/codex/260907_codex_05.md
```

Frozen design baseline prepared by ChatGPT:

```text
03d7b7cdb1663504c60e2ae4cbdfbf083080651b
```

Pinned collaboration authority:

```text
cigit-zgy/agent-collaboration@ad88170b23920ddac0bff9a2fd467aa0c59917cf
```

If local execution exposes a genuine design ambiguity in `ask_human`/`notify_human`, checkpoint classification, machine-wide activation, verdict ordering, or failure semantics, classify `DESIGN_GAP`, stop the affected implementation path, and return `BLOCKED`. Do not patch semantics locally.

## Shared coding Skills and authoring state

Use the task-pinned shared coding-Skill profile and the repository's current Rust/TypeScript tooling. Do not add a new dependency or server framework when the existing validated MCP/runtime can implement the accepted contract directly.

ChatGPT has already authored and committed:

```text
reports/concept/260908_concept_01.md
design/05_mcp_interface.md
design/06_codex_integration.md
design/README.md
design/00_overview.md
SKILL.md
AGENTS.md
```

Codex owns only local implementation, machine integration, bounded repair, and verification from this committed contract.

## LOCAL scope

### 1. Establish the exact local execution baseline

Locate the User's local checkout whose Git origin resolves to:

```text
cigit-zgy/human-in-loop
```

Do not guess by folder name alone.

All Agent-created task-local state belongs under:

```text
<PROJECT_ROOT>/tmp/HUMAN_IN_LOOP_CODEX_HITL_06/
```

Use a linked task worktree under that boundary when useful.

Before any repository mutation:

```text
fresh git fetch --prune
→ resolve origin/task/260908-codex-hitl-global-integration
→ verify committed task exists on fetched branch
→ inspect branch / HEAD / upstream / worktree / existing User state
→ establish safe task worktree
→ prove local execution HEAD == fetched authorized task commit
```

Do not use blind `git pull`, destructive reset, rebase, automatic stash, force checkout, or force push to achieve synchronization.

### 2. Extend the existing production MCP surface

Reuse the production server/runtime already qualified by `HUMAN_IN_LOOP_MCP_05`.

Public maintained MCP tools must become exactly:

```text
ask_human
notify_human
```

Preserve the current `ask_human` behavior and its existing Level-3 contract.

Implement `notify_human` exactly from `design/05_mcp_interface.md`:

```text
non-blocking informational dispatch
no choices
no pending human decision
no wait for incoming acknowledgement
bounded dispatch result
no transport secrets in schema/result/logs
```

Do not implement notification as a fake `ask_human` confirmation with an acknowledgement choice.

Reuse existing repository identity and configured channel transport/rendering infrastructure. Introduce the smallest separate notification path needed to keep decision and notification semantics explicit.

Do not add a second MCP server, second confirmation coordinator, GitHub relay, public HTTP endpoint, cloud backend, or generic command/file/message control surface.

### 3. Deterministic MCP and regression tests

Add/adjust focused tests so the public production contract proves at least:

```text
tools/list exposes exactly ask_human + notify_human
both schemas are closed
ask_human schema/result/cancellation regressions remain green
notify_human status/summary/task/locator validation
notify_human creates no pending confirmation request
notify_human does not wait for a human response
notify_human returns bounded non-sensitive dispatch status
repository identity remains canonical
no recipient/chat/credential/raw-message/generic-command fields are exposed
iMessage-only / same-account / strict-correlation decision regressions remain green
Feishu/coordinator regressions remain green
server cancellation/shutdown leaves no orphaned request/watcher/process state
```

Use design-probing tests. If a test requires semantics not determined by the committed design, classify that as `DESIGN_GAP` instead of inventing a production special case.

### 4. Inspect the actual Codex host before machine changes

Determine the effective current Codex installation/configuration rather than assuming paths or syntax:

```text
resolve CODEX_HOME (default ~/.codex only when actually effective)
record codex version
inspect current $CODEX_HOME/AGENTS.md if present
inspect current $CODEX_HOME/AGENTS.override.md if present
inspect current $CODEX_HOME/config.toml if present
inspect currently supported MCP registration/config syntax
inspect currently supported Skill discovery location/mechanism
```

Use current installed Codex behavior plus the pinned task/OpenAI-supported mechanisms. Do not replace user configuration wholesale.

Before changing machine-global files, save exact pre-change copies/absence markers under:

```text
<PROJECT_ROOT>/tmp/HUMAN_IN_LOOP_CODEX_HITL_06/recovery/
```

Keep these untracked until ChatGPT acceptance because they have explicit rollback value. Never copy secrets into the repository/report.

### 5. Install the Skill as a pinned discovery authority

Expose the committed repository-root `SKILL.md` through the current supported user-scoped Codex Skill discovery mechanism.

The machine policy must bind the Skill to the immutable task source containing the accepted Skill semantics:

```text
cigit-zgy/human-in-loop@<TASK_SOURCE_SHA>:SKILL.md
```

where `<TASK_SOURCE_SHA>` is the commit containing this task artifact.

A local discovery copy/cache must be proven content-equal to that exact committed Skill. Do not point global policy at an unpinned moving `master/latest` checkout.

Preserve unrelated existing user-scoped Skills/discovery entries.

### 6. Add the thin machine-wide Codex AGENTS rule

Modify the effective `$CODEX_HOME/AGENTS.md` surgically. Preserve all unrelated existing User/collaboration instructions.

Add only a concise human-in-loop section equivalent in semantics to:

```text
For every Codex task, apply the pinned cigit-zgy/human-in-loop Skill.
At a mandatory human checkpoint, stop the affected path and use ask_human; do not continue without one valid correlated human result.
Do not substitute assumptions/defaults/timeouts for a required decision.
Project/task rules may add checkpoints but do not silently bypass the global contract.
At every normal terminal task state, after the normal result and required repository synchronization, attempt notify_human before the final Codex response.
If a mandatory ask_human capability is unavailable, BLOCKED and do not cross the checkpoint.
A notify_human failure does not change the underlying task verdict; surface HUMAN_NOTIFICATION: FAILED locally.
```

Do not copy the full classifier, MCP schema, iMessage grammar, transport configuration, or design documents into `$CODEX_HOME/AGENTS.md`.

Do not put private recipients, credentials, tokens, or phone identifiers in AGENTS.

### 7. Register the production MCP server in Codex config

Modify the effective `$CODEX_HOME/config.toml` using the currently supported MCP configuration form.

Register the production human-in-loop MCP server/command so a fresh Codex session receives the maintained tools.

Requirements:

```text
preserve all unrelated existing config
no duplicate competing human-in-loop server registrations
no secrets committed or copied into AGENTS/SKILL
no public network listener required
use the installed/built production server, not a test shim
```

Private channel recipient/configuration remains in the existing human-in-loop application/local configuration owner.

### 8. Production MCP-client E2E

Against the final production server, use a real MCP client/protocol client and verify:

```text
initialize
→ tools/list == ask_human + notify_human
```

Then run one harmless `ask_human` request through the production path and obtain one real correlated human choice through the configured phone channel. Preserve current same-account iMessage safety if iMessage participates.

Run one harmless terminal-style `notify_human` message and prove:

```text
production MCP call
→ configured delivery path
→ phone receives compact notification
→ MCP call returns without waiting for a human reply
→ no pending decision request remains
```

Record only redacted structural evidence.

### 9. Fresh Codex harness E2E — global policy actually works

Start a **new/fresh Codex process/session after the machine configuration is installed**. Do not reuse the current pre-configuration Codex context as the only proof.

Use a bounded disposable test workspace under the project task tmp boundary or another safe project-owned test location.

Prove all of the following through actual Codex behavior:

```text
A. fresh Codex loads the machine-wide AGENTS rule
B. the pinned human-in-loop Skill is discoverable/resolved at the intended exact authority
C. the production human-in-loop MCP tools are available
D. a deliberately declared harmless EXPLICIT_CHECKPOINT causes Codex to call ask_human and wait rather than guessing
E. the returned stable human choice controls the continuation
F. after the test task reaches its terminal result, Codex attempts notify_human automatically
G. notify_human does not require an acknowledgement to let Codex terminate
```

Do not weaken the global policy simply to make this E2E easier.

### 10. Fail-closed / failure-semantics evidence

Provide deterministic or isolated evidence that:

```text
mandatory decision + unavailable/failed ask_human
→ affected operation is not executed
→ task becomes BLOCKED

established task verdict + failed/unavailable notify_human
→ established verdict is preserved
→ HUMAN_NOTIFICATION failure is surfaced separately
```

Prefer an isolated test configuration/process or existing test hooks; do not intentionally break the User's live global configuration without reliable automatic restoration.

### 11. Full regression / security verification

Because this modifies a public MCP control surface and machine-wide Codex behavior, run Level-3 evidence proportionate to the final codebase, including the repository's existing final-state gates from the prior MCP qualification:

```text
focused MCP tests
full Rust tests
cargo fmt --check
cargo clippy --all-targets -- -D warnings
frontend/Vitest/Node tests as applicable
frontend production build
Tauri production build/install/smoke as applicable
installed production MCP protocol verification
privacy/security scan focused on MCP schemas/results/logging and machine config
real phone E2E for ask_human + notify_human
fresh Codex harness E2E
```

Reuse existing scripts/test infrastructure rather than duplicating it.

## Non-goals / ownership boundary

Do not:

```text
change current decision/channel scientific/product semantics outside design/05 + design/06
add Telegram/Slack/DingTalk/WeChat/SMS/MMS/RCS or another delivery channel
add carrier fallback
add GitHub runtime relay
publish a public MCP endpoint
create a cloud backend
expose generic shell/file/Messages/credential controls
change repository visibility
merge master
merge task/260907-mcp-interface into master
create tag/release
replace the User's entire Codex AGENTS/config files
rewrite unrelated User machine configuration
add project-specific checkpoints to the global Skill merely for testing
```

## Acceptance criteria

PASS requires all of the following:

1. Final implementation conforms to `design/05_mcp_interface.md`, `design/06_codex_integration.md`, and `SKILL.md` without new task-local semantics.
2. Public production MCP discovery exposes exactly `ask_human` and `notify_human`.
3. `ask_human` retains its previous qualified blocking/correlation/cancellation/security behavior.
4. `notify_human` is genuinely non-blocking, requires no reply, creates no fake pending decision, and returns only bounded non-sensitive dispatch data.
5. Existing iMessage-only/same-account/correlation and Feishu/coordinator behavior does not regress.
6. The effective Codex-home AGENTS file contains only the thin global activation/failure rule and preserves unrelated existing instructions.
7. The effective Codex config registers the production MCP server without replacing unrelated configuration or exposing secrets.
8. The Codex Skill discovery entry resolves to the exact immutable task-source Skill content and does not silently follow an unpinned moving checkout.
9. A fresh Codex process actually invokes `ask_human` at a declared checkpoint and resumes only from the returned stable human choice.
10. The same fresh Codex lifecycle automatically attempts `notify_human` at terminal state and terminates without acknowledgement.
11. Mandatory decision-tool unavailability fails closed; notification failure preserves the established task verdict.
12. Required Level-3 regression/build/install/security/privacy gates pass.
13. No private recipient/message contents/credentials are committed or written into the report.
14. Repository changes are pushed and final fresh-fetch proves local task HEAD == fetched upstream task-branch HEAD.
15. Machine-global pre-change recovery copies remain untracked and recoverable until ChatGPT acceptance.

## Git / synchronization / integration

Work only on:

```text
task/260908-codex-hitl-global-integration
```

The repository task starts from the exact fetched task commit containing this specification, not from stale local `master` or a stale copy of `task/260907-mcp-interface`.

Before mutation and after final push, obey the pinned collaboration two-sided remote synchronization handshake exactly.

Do not merge `master`, change visibility, or create a release/tag.

Post-acceptance integration remains `USER_CHECKPOINT` because this task changes machine-wide Codex policy/configuration and a public local MCP control surface.

## Codex report

Write exactly:

```text
reports/codex/260908_codex_01.md
```

The report must include:

```text
task source SHA
initial authorized baseline evidence
final task branch HEAD
fetched upstream task branch HEAD
TASK_HEAD_EQUALS_UPSTREAM
public MCP tools/schema summary
machine-wide AGENTS/config change summary without secrets
pinned Skill authority + local discovery equality evidence
repository regression evidence
real ask_human E2E evidence
real notify_human E2E evidence
fresh Codex global-policy E2E evidence
fail-closed / notification-failure evidence
local recovery/tmp disposition
limitations
```

Use the task-pinned collaboration Codex-report contract.

After the report commit is pushed and final remote equality is verified, attempt the **newly installed `notify_human` terminal notification for this task itself**. This self-notification is the final live integration evidence; its success/failure is reported in the final Codex response and does not rewrite the already-established task verdict.

Return the normal FORMAL compact locator only.
