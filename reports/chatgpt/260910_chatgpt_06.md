# HUMAN_IN_LOOP_RELEASE_V013_21

## Task type

FORMAL RELEASE / INSTALL / PIN / PUBLISH TASK

## Repository

`cigit-zgy/human-in-loop`

## Task branch

`task/260910-release-v0.1.3`

## ChatGPT acceptance

`PASS`.

Accepted execution evidence:

- implementation commit: `7dd8782823037ed944d9bfd031b85493695c47bd`
- Codex report: `reports/codex/260910_codex_05.md`
- report commit: `a6f738ef73a6a6e70187a27979710f23e425add3`
- accepted task: `reports/chatgpt/260910_chatgpt_05.md@f40e4829c8060369c3ad5818337a7ac0ed34773b`

The accepted live qualification returned exactly one canonical MCP result with `selected_choice_id=received_correctly` and `source_channel_id=imessage`, after one real send, with complete watcher/request/client cleanup. The legacy tray/updater was retired from production, Feishu is absent, headless daemon/MCP/Bot-worker operation passed, and bounded history catch-up preserved the original strict correlation ledger.

## Pinned collaboration authority

`cigit-zgy/agent-collaboration@8601466216515125bf8b17893b2a8e8673bab79e`

Follow the normal FORMAL lifecycle: fresh-fetch first; inspect branch/HEAD/upstream/worktrees; preserve unrelated User state; task scratch only under `<PROJECT_ROOT>/tmp/HUMAN_IN_LOOP_RELEASE_V013_21/`; no destructive reset/force-push/history rewrite; commit/push/fresh-fetch proof; durable Codex report.

## Standing release authorization

The User has explicitly authorized routine release after ChatGPT acceptance. Do not call `ask_human` merely to reconfirm merge, installation, pin, tag, or GitHub Release publication for this accepted release.

A genuinely new security/privacy/licensing/repository-visibility/destructive boundary still stops normally. No such boundary is currently known.

## Release objective

Publish `human-in-loop v0.1.3` from the accepted candidate without reopening product design.

The release must preserve these accepted v0.1.3 semantics:

```text
maintained remote channel = imessage only
public MCP tools = ask_human + notify_human only
legacy Feishu runtime/config/Keychain path absent
legacy AskHuman tray/menu-bar production host absent
legacy Naituw/AskHuman updater path absent
headless daemon + Codex MCP + dedicated Bot worker retained
numeric correlation reply = <TOKEN>-<OPTION>, normal token 5 decimal digits
strict chat/send-row/GUID/token/option/terminal correlation retained
bounded history catch-up uses the same correlation resolver
quoted report locator stays in one message and avoids the qualified large link preview
default progress notifications = 0
one logical terminal task <= one terminal notify_human attempt
optional dedicated-Bot automatic login remains opt-in
explicit iMessage-only send + --no-sms-fallback retained
canonical HOME / stable signing / TCC boundaries retained
```

## Scope

### 1. Release metadata only

Update all maintained product version surfaces from `0.1.2` to `0.1.3` where required for a coherent release (for example Tauri/Cargo/package/release metadata actually used by the maintained build/install path).

Do not revive or synchronize version metadata for retired AskHuman GUI/updater surfaces merely because historical files contain old values.

Prepare concise v0.1.3 release notes covering the accepted changes:

- iMessage-only maintained product; Feishu removed from current runtime/config/dependencies.
- decimal `TOKEN-OPTION` reply grammar.
- compact quoted report locator qualified without large Rich Link Preview and remaining tappable.
- terminal notifications quiet by default: zero routine progress, at most one terminal notification.
- simplified onboarding and opt-in dedicated-Bot automatic login.
- legacy visible AskHuman tray/login startup and upstream updater retired.
- bounded Bot-owned history reconciliation/catch-up for watcher misses, without weakening correlation.

Do not include private recipient/chat/token/credential data.

### 2. No new production redesign

After the accepted implementation, production/runtime behavior may change only as needed for release-version metadata or a strictly mechanical release correction.

If any substantive runtime/transport/correlation/security behavior must change, STOP and report `BLOCKED`; do not silently include a new fix in the release.

### 3. Final release-tree verification

Run the material release gates on the exact final release tree before publication. At minimum:

```text
cargo test
cargo fmt --check
cargo clippy --all-targets -- -D warnings
pnpm test
pnpm build
pnpm tauri build --no-bundle
Skill validation
production MCP synthetic verifier
security/privacy/diff checks appropriate to changed surfaces
```

Prove again:

```text
maintained channels exactly [imessage]
MCP tools exactly ask_human + notify_human
Feishu runtime/config/keyring path absent
legacy GUI auto-launch/updater unreachable
numeric TOKEN-OPTION regression PASS
bounded catch-up/cancellation/duplicate/late regression PASS
rich multiline detail PASS
quoted report locator regression PASS
no SMS/MMS/RCS/carrier fallback
```

The accepted real phone round trip from implementation commit `7dd8782...` may be reused as release evidence only if final production runtime/transport code is content-equivalent apart from release/version metadata. Do not send another real `ask_human` merely to duplicate evidence. If substantive runtime code changes, do not reuse it; stop for a new acceptance boundary.

### 4. Integrate to master

The release branch contains the accepted candidate ancestry. After final verification, create the minimal release-content commit containing the v0.1.3 release metadata/notes.

Then update `master` by fast-forward only to that verified release-content commit. Before push, fresh-fetch `origin/master` and require it still descends exactly from the known pre-release master line; if a non-fast-forward integration would be required because of unexpected external changes, STOP rather than force.

Do not squash away the accepted design/task/report history. Do not force-push.

Record the exact final release-content commit as `RELEASE_COMMIT`.

### 5. Install exact release runtime

Build from the exact `RELEASE_COMMIT`, stable-sign and install through the maintained routine update path.

Verify on the installed production path:

```text
reported version = 0.1.3
legacy AskHuman tray/menu-bar host not running and not auto-launched
legacy upstream AskHuman updater not available
headless daemon starts unattended
MCP initialize/list succeeds with exactly two tools
Bot worker ready
iMessage health ready
Setup COMPLETE
no SecurityAgent/Keychain prompt caused by retired Feishu path
canonical runtime HOME shared correctly
stable signing/designated requirement valid
```

Do not reboot, reset TCC, sign out Apple Account, recreate Bot user, alter recipient, disable FileVault/SIP, or delete old Feishu Keychain credentials.

### 6. Machine-wide Skill pin

Update the maintained machine-wide human-in-loop Skill authority to the exact `RELEASE_COMMIT`:

`cigit-zgy/human-in-loop@<RELEASE_COMMIT>:SKILL.md`

Verify the user discovery copy is content-equal to `SKILL.md` at that commit. Do not leave the machine pinned to v0.1.2 or an intermediate task/report commit.

### 7. Tag and GitHub Release

Create annotated tag:

`v0.1.3`

The tag MUST peel exactly to `RELEASE_COMMIT`, not to a later report-only commit.

Create a public GitHub Release:

```text
name: human-in-loop v0.1.3
draft: false
prerelease: false
assets: none (source-only)
```

Release notes must match the final accepted product and must not claim unsupported real-world evidence.

### 8. Final report

After tag/release publication, write:

`reports/codex/260910_codex_06.md`

If occupied, use the next free `260910_codex_XX.md` and explain.

Report at minimum:

```text
VERDICT
TASK_SOURCE
CHATGPT_ACCEPTANCE
ACCEPTED_IMPLEMENTATION_COMMIT
ACCEPTED_REPORT_COMMIT
RELEASE_COMMIT
MASTER_HEAD_AFTER_RELEASE
TAG
TAG_OBJECT
TAG_TARGET
GITHUB_RELEASE
RELEASE_ASSETS
INSTALLED_VERSION
INSTALLED_BINARY_MATCH
LEGACY_TRAY_STATE
LEGACY_UPDATER_STATE
MAINTAINED_CHANNELS
FEISHU_REMOVAL_REGRESSION
MCP_SURFACE
DAEMON_STARTUP
BOT_WORKER_HEALTH
IMESSAGE_HEALTH
SETUP_STATE
NUMERIC_TOKEN_REGRESSION
HISTORY_CATCHUP_REGRESSION
NO_SMS_REGRESSION
RICH_BODY_REGRESSION
LINK_PRESENTATION_REGRESSION
AUTOLOGIN_REGRESSION
HOME_MIGRATION_REGRESSION
SKILL_PIN
SKILL_DISCOVERY_COPY_MATCH
TEST_SUMMARY
SECURITY_SUMMARY
PRIVACY_SUMMARY
FRESH_FETCH_PROOF
HUMAN_NOTIFICATION
```

Commit/push/fresh-fetch the report after publication. The report commit is allowed to be later than the tag target and MUST NOT change the meaning of `RELEASE_COMMIT`.

### 9. Terminal notification

After the release report is durable and fresh-fetched, attempt exactly one terminal `notify_human` for this logical release task. It should state that v0.1.3 is published and use the immutable release/report locator. Do not send separate progress/release/tag notifications.

## STOP conditions

STOP and report `BLOCKED` if:

- accepted runtime code would need substantive redesign/change;
- final full regression fails;
- Feishu/tray/updater production capability reappears;
- iMessage/MCP/Bot worker/setup health regresses;
- stable signing/TCC/canonical-HOME invariants fail;
- master cannot be updated by safe fast-forward because of unexpected remote divergence;
- tag `v0.1.3` already exists with a different target;
- release publication would require destructive history/repository/security changes.

## Completion

PASS only when `v0.1.3` is built from one exact verified `RELEASE_COMMIT`, master contains it, the installed runtime reports 0.1.3 and remains headless/iMessage-only/healthy, the machine-wide Skill is pinned to that exact commit, annotated tag `v0.1.3` peels to it, a public source-only GitHub Release exists, the immutable release report is pushed/fresh-fetched, and exactly one terminal notification attempt is recorded.