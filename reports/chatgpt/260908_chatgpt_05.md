# HUMAN_IN_LOOP_RELEASE_V010_10

## Task type

FORMAL RELEASE TASK

## Repository

`cigit-zgy/human-in-loop`

## Accepted release candidate

`91f88559a933188473c0c4d79c439f77103448b5`

ChatGPT acceptance: **PASS**.

The User has explicitly authorized publication of the first independent public release as **`v0.1.0`**. Do not ask again for authorization to merge, tag, or create the GitHub Release when all acceptance checks below pass. A new `ask_human` checkpoint is permitted only if execution exposes a materially new semantic/design/security/publication decision not already resolved here.

## Pinned collaboration authority

Follow `cigit-zgy/agent-collaboration@8601466216515125bf8b17893b2a8e8673bab79e` exactly.

In particular:

- fetch before acting;
- inspect branch/HEAD/upstream/worktree and preserve User state;
- keep task scratch only under `<PROJECT_ROOT>/tmp/<WORK_ID>/`;
- no reset/force-push/destructive history rewrite;
- commit scoped changes, push, fresh-fetch, and prove local HEAD equals fetched upstream where required;
- release verification must be evidence-based rather than ceremonial;
- final durable report goes under `reports/codex/`;
- terminal human notification is attempted only after durable Git/release truth exists.

## Release decision already made

The release is:

```text
version: v0.1.0
scope: first independent public source release
binary-distribution claim: NO
notarized Developer ID installer claim: NO
progress-notification feature: deferred to v0.1.1; do not add it here
```

Do not broaden this release into a new feature cycle.

## Why this task exists

The accepted RC is functionally qualified, including real production MCP → iMessage → iPhone → correlated reply evidence, repeat installation, stable macOS permission identity, and security checks. Publication is blocked only by release hygiene and default-branch integration.

Known Git fact at task issuance:

- remote `master` = `f5a890ab19ec2af2810c0a4bc996c43aa3bf8369`;
- accepted RC = `91f88559a933188473c0c4d79c439f77103448b5`;
- the histories are diverged;
- `master` has two independent documentation-history commits after the earlier common base, with net content returning to that base;
- therefore **do not force-update master**. Preserve both histories through a normal merge/integration path.

Re-fetch and re-check this fact at execution time. If remote state has materially changed, classify it rather than assuming these SHAs are still current.

# Required work

## 1. Recover and verify repository state

Before editing:

1. `git fetch --all --prune` or the repository-authorized equivalent.
2. Verify current task branch, local HEAD, tracked upstream, default branch, worktree cleanliness, and any unrelated User worktrees/state.
3. Verify accepted RC `91f885...` is in the task branch ancestry and inspect current remote `master` divergence.
4. Do not delete or rewrite unrelated worktrees/branches.

If a clean task worktree is needed, create it only under `<PROJECT_ROOT>/tmp/<WORK_ID>/` and remove it with `git worktree remove` when complete.

## 2. Release identity and version hygiene

Set the independent project release version to **`0.1.0`** consistently across every distributable/version-bearing manifest that belongs to this repository, including at minimum:

- root `package.json`;
- `src-tauri/Cargo.toml`;
- `src-tauri/tauri.conf.json`;
- `packaging/npm/humaninloop/package.json`;
- all `packaging/npm/platforms/*/package.json` package versions;
- internal optional-dependency version references among those npm manifests;
- lockfile/version fields only when actually required by the package manager after the manifest change.

Audit release metadata for inherited upstream product authorship. The public project identity is `cigit-zgy/human-in-loop`, not `Naituw/AskHuman`.

Required policy:

- project/package `author` metadata must no longer present `Naituw` as the author of this independent release;
- upstream AskHuman credit remains explicit and complete in `THIRD_PARTY.md` and `LICENSES/AskHuman-MIT.txt`;
- do not delete or weaken upstream license attribution;
- do not rewrite historical report text merely because it names AskHuman;
- do not perform a broad cosmetic rebrand of inherited internal code in this release.

For npm packaging identity, audit the existing `packaging/npm/humaninloop` surface. Make the **smallest release-safe correction** needed so a future package build does not falsely present the independent package as the upstream AskHuman product. Do not introduce an untested compatibility break merely for cosmetics. Any intentionally retained legacy command/package alias must be explicitly identified as compatibility, not primary product identity, and must be covered by existing/added packaging smoke tests.

## 3. README release-state update

Update `README.md` from release-candidate wording to the actual `v0.1.0` public state.

README must clearly state:

- `v0.1.0` is the first independent public release;
- the production-qualified macOS path is the distinct-account dedicated-Bot-user iMessage topology already documented;
- setup is one-time and repeat normal operation remains unattended subject to the documented reboot/Bot-login lifecycle boundary;
- this release is a **source release**;
- it does **not** claim a notarized downloadable macOS installer or general binary distribution;
- exactly two maintained remote channels remain Feishu and iMessage-only Apple Messages;
- `ask_human` is blocking semantic decision; `notify_human` is non-blocking informational delivery;
- terminal task notification is the default operational behavior;
- periodic/progress notification is **not** part of `v0.1.0` and must not be advertised as implemented.

Keep README concise. Do not turn it into a design dump; link to `design/` for details.

## 4. Human checkpoint policy for released Skill

Audit `SKILL.md` against the accepted policy and the pinned AskHuman autonomous collaboration semantics.

The released behavior must remain:

```text
ASK HUMAN only when an unresolved semantic decision actually exists:
- explicit User/project checkpoint;
- materially different valid outcomes requiring User preference;
- destructive/irreversible action;
- meaningful external/public side effect not already authorized;
- material authority/design/scientific/product-semantics change;
- genuinely new security/credential/trust boundary.

DO NOT ask again for:
- already-authorized release/merge/tag in this task;
- routine reversible implementation details;
- normal fetch/commit/push/test/build/install/health operations authorized by task;
- facts discoverable from the codebase/repository.
```

This is intentionally aligned with the AskHuman upstream **autonomous** posture: reasonable defaults; fewer interruptions; ask when blocked/high blast-radius/irreversible/security-sensitive or when the User explicitly needs to decide; material plan/scope/user-visible-behavior changes require asking.

Do not import AskHuman's old mandatory `whats-next`/end-of-turn blocking behavior into this project. The human-in-loop release policy is:

```text
normal task completion -> notify_human once
not -> force a new blocking "what next?" decision
```

If `SKILL.md` already encodes this correctly, do not churn it merely to change wording. Change only if a real semantic gap exists.

## 5. Release notes

Create a concise user-facing `v0.1.0` release note in a stable repository location suitable for reuse as the GitHub Release body. Prefer `docs/release-notes/v0.1.0.md`; create the minimal `docs/release-notes/` structure only if absent.

Release notes should cover:

- first independent human-in-loop release;
- local stdio MCP tools: `ask_human`, `notify_human`;
- Feishu and strict iMessage-only Apple Messages;
- production macOS dedicated Bot user + distinct Apple Account topology;
- one-time bootstrap and stable post-setup permission identity;
- real iPhone notification/reply qualification;
- terminal Codex notification integration;
- known reboot lifecycle boundary;
- source-release-only limitation;
- upstream AskHuman/imsg attribution via `THIRD_PARTY.md`.

No inflated claims. No private addresses, Messages content, local usernames, credentials, or host-specific secrets.

## 6. Default-branch integration

Integrate the accepted RC + release-hygiene changes into the latest remote `master` **without rewriting history**.

Required:

- fetch immediately before integration;
- preserve the current master-only history;
- use a normal merge/PR or equivalent non-force integration;
- no `reset --hard` to replace master;
- no force-push;
- resolve conflicts only from repository authority; if a conflict represents a new semantic decision, STOP and use `ask_human` rather than guessing.

After merge, fresh-fetch and prove:

```text
local master HEAD == fetched origin/master
```

and verify the release commit/tag target is exactly the intended integrated master state.

## 7. Machine-wide Codex integration pin

After the final release commit on `master` is known, update the existing machine-wide human-in-loop Codex integration to pin/discover the released `SKILL.md` from that exact immutable release revision, while preserving unrelated `$CODEX_HOME` content.

Do not reinstall or rewrite unrelated Codex configuration.

Verify through the actual installed/production MCP boundary that:

```text
tools/list -> exactly ask_human + notify_human
```

and that the Skill authority/pin resolves to the final release revision rather than a stale task/RC commit.

If the machine-wide pin mechanism is already release-tag based and resolves immutably to the correct final target, verify rather than rewrite.

## 8. Final release verification

Because functional code was already Level-3 qualified at the accepted RC, do not gratuitously repeat every destructive/expensive qualification. But release integration and metadata changes must receive material smoke evidence.

At minimum run/verify:

- manifest/version consistency check for `0.1.0`;
- Rust formatting/check/tests appropriate to changed manifests and merged tree;
- frontend/package tests/build appropriate to changed manifests;
- production build/install smoke sufficient to prove version/metadata changes did not break the installed `human-in-loop mcp` path;
- installed MCP `tools/list` exactly `ask_human`, `notify_human`;
- non-destructive channel/setup health check;
- repository privacy scan for private addresses/Messages content/credentials in new release artifacts;
- dependency/security checks required by the current release contract when lockfiles changed.

Do **not** send another real qualification iMessage solely because the version number changed if the qualified identity/route is unchanged and the current setup contract correctly says no requalification is required. A harmless `notify_human` terminal publication notification after release is sufficient.

Any regression in the real release tree that invalidates the accepted RC -> STOP publication, report `BLOCKED` or `FAIL`, and do not create tag/release.

## 9. Tag and GitHub Release

Only after master integration and final verification pass:

1. Create annotated or repository-standard tag **`v0.1.0`** at the exact verified release commit on `master`.
2. Push the tag normally, no force.
3. Create a public GitHub Release for `v0.1.0` using the committed release notes.
4. This is **not** a prerelease.
5. Do not attach an unnotarized app/binary and do not imply downloadable binary support. Source archives generated by GitHub are sufficient for this release.
6. Verify by fresh GitHub fetch/API/`gh` that:
   - tag exists;
   - tag target is the verified master release commit;
   - GitHub Release exists and is public/non-prerelease;
   - README on default branch reflects released state.

## 10. Final report and notification

Write the execution report to:

`reports/codex/260908_codex_05.md`

If that exact path is already occupied at execution time, use the next free `260908_codex_XX.md` and state why.

Report must include at least:

```text
VERDICT
RELEASE_VERSION
RELEASE_COMMIT
MASTER_HEAD
TAG_TARGET
GITHUB_RELEASE
README_UPDATED
VERSION_CONSISTENCY
PACKAGE_IDENTITY_HYGIENE
UPSTREAM_ATTRIBUTION_PRESERVED
MACHINE_SKILL_PIN
INSTALLED_MCP_TOOLS
TEST_SUMMARY
SECURITY_SUMMARY
PRIVACY_SUMMARY
WORKTREE_CLEANUP
FRESH_FETCH_PROOF
```

Ordering at terminal PASS:

```text
release code/docs complete
-> commit/push
-> integrate master
-> fresh-fetch proof
-> tag + GitHub Release
-> verify public release
-> update/verify machine-wide Skill pin
-> final durable report commit/push if repository rules permit post-release report commit without moving the tag
-> notify_human once with compact RELEASED/PASS summary using the supported terminal status contract
-> final Codex response
```

The `v0.1.0` tag must remain on the verified release tree; a later report-only commit may advance `master` if necessary, but must not move the tag.

Notification failure must not rewrite an otherwise valid release PASS; report it separately as `HUMAN_NOTIFICATION: FAILED`.

# STOP conditions

STOP before publication if any of the following occurs:

- accepted RC ancestry cannot be established;
- remote master has material new code/semantics that cannot be safely reconciled from authority;
- release merge requires destructive history rewrite;
- tests/build/install/MCP smoke expose a release regression;
- package identity correction would require a material compatibility decision not resolved by current authority;
- tag `v0.1.0` already exists but points somewhere else;
- a public `v0.1.0` Release already exists with a conflicting target/state;
- machine-wide pin cannot be updated/verified without risking unrelated Codex configuration;
- any private address, message content, credential, or other prohibited private material would enter Git/release artifacts.

# Final acceptance

PASS only when all of the following are simultaneously true:

```text
accepted RC preserved
+ release metadata/version = 0.1.0
+ README says released source-release state
+ upstream attribution preserved
+ master integrated without history rewrite
+ final release smoke passes
+ v0.1.0 tag points to verified release commit
+ public non-prerelease GitHub Release exists
+ machine-wide Codex Skill pin resolves to the immutable release revision
+ installed MCP still exposes exactly ask_human + notify_human
+ no private data leaked
+ terminal notify_human attempted
```
