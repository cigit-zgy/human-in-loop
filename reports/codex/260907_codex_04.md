---
artifact_type: codex_report
artifact_id: 260907_codex_04
task_id: HUMAN_IN_LOOP_PROJECT_LABEL_04
title: Require GitHub repository identity in compact iMessage notifications
date: 2026-09-07
project: human-in-loop
repository: cigit-zgy/human-in-loop
status: completed
summary: >
  Implemented the compact bounded iMessage confirmation renderer with mandatory
  GitHub repository-slug identity, fail-closed repository resolution, and a
  successful single-send same-account phone round trip.
verdict: PASS
task_branch: task/260907-imessage-project-label
task_source_sha: 82d0c227e8997e00bac993f4b24c82351aa2b3c8
baseline_sha: a9d643f7c5dc7cd5d2397ca47a21ba2559a854c0
verification_level: level_3
limitations: []
---

# Require GitHub repository identity in compact iMessage notifications — Codex report

## Changes

- Stopped the superseded `260907_chatgpt_03` execution before its real E2E or any commit, then reused only its conforming uncommitted compact-renderer work under this task.
- Replaced the verbose iMessage confirmation body with the accepted compact shape: HIL/token header, source/repository line, at most two compact context lines, one compact question, numbered choices, and the exact reply instruction.
- Enforced the current Unicode-scalar budgets: source/repository line 80, context line 80, question 160, choice label 60, 2–6 choices, and total text 700. Required content is never truncated; unsupported requests send nothing through iMessage.
- Added canonical repository identity resolution to the existing project module. GitHub HTTPS, SCP-style SSH, and `ssh://` origin URLs resolve to the repository slug only. Local directory, worktree, branch, task, URL, and owner identity are not rendered.
- Distinguished a genuine non-repository interaction from a Git repository whose canonical GitHub origin is unavailable. Only the former may omit the repository label; the latter fails closed before sending.
- Kept the established iMessage transport and correlation path unchanged: explicit iMessage service, no SMS fallback, post-send boundary, GUID/token/request checks, same-account handling, distinct-peer self rejection, exactly-one terminal result, and watcher cleanup.
- Updated the ignored live release-candidate test to use the canonical production renderer, resolve the current repository through its GitHub origin, and assert exactly one application send plus the stable canonical choice.

## AI-assisted implementation transparency

The committed task and living design were the task authority. Codex authored the implementation, deterministic tests, local verification setup, and this report. Compact-renderer work created during the stopped task was re-audited against baseline `a9d643f7c5dc7cd5d2397ca47a21ba2559a854c0`; repository identity and fail-closed behavior were added under this task. No generated private message body, token, recipient, chat identity, or runtime configuration was copied into tracked artifacts.

The implementation deliberately uses the existing canonical project path carried by `ConfirmEntry`. It asks Git for that root's `origin` URL and performs a small pure parser step; it does not add a repository registry, cache, manifest, or iMessage-specific identity model.

## Shared coding-Skill alignment

- Resolved and followed `cigit-zgy/agent-collaboration@8601466216515125bf8b17893b2a8e8673bab79e` for FORMAL execution, Level-3 verification, reporting, exact-path staging, and push/fresh-fetch handoff.
- Verified the pinned Ponytail `full` profile at `2ed6c52c9d7e5e56942508591085fd45dea277d3` and kept the change to one existing project helper plus the renderer integration.
- Used test-driven development: new repository identity and mandatory-label tests failed before implementation, then passed. A later fail-closed audit added a red test distinguishing unresolved GitHub identity from non-repository context before the final implementation.
- Used systematic debugging for the task-local pnpm layout and macOS Trash timeout, and verification-before-completion before commit/handoff.

## Verification evidence

### Deterministic and build gates

- Focused repository identity tests: 7 passed. Coverage includes GitHub HTTPS and SSH URLs, slug-only output, canonical remote preference over a task/worktree directory name, non-repository omission, and unresolved-repository fail-closed behavior.
- Focused iMessage tests: 26 passed. Coverage includes compact shape, exact reply grammar, repository-label preservation and exact 80-scalar boundary, hard total limit, iMessage-only send arguments, same-account correlation, distinct-peer self rejection, and watcher boundary behavior.
- Full Rust suite on the final code: 1,205 passed, 3 ignored, 0 failed. The ignored set includes the explicitly invoked live phone test.
- `cargo fmt --check`: passed.
- `cargo clippy --all-targets -- -D warnings`: passed. Cargo emitted only the existing future-incompatibility notice for transitive `block 0.1.6`.
- Frontend Vitest: 28 files and 178 tests passed.
- Node verification: 9 tests passed; a final channel-scope smoke rerun passed 4/4.
- Frontend typecheck and production Vite build: passed. Vite retained its existing large-chunk advisory.
- Tauri production build with `--no-bundle`: passed and produced the release application binary.
- Isolated `scripts/install.sh --release`: passed; installed `AskHuman v0.13.1`, verified signing, and exposed only maintained `feishu | imessage` channel help with SMS fallback disabled.
- Semgrep 1.176.0 with 11 Rust rules across the three touched source files: 0 findings.
- Final privacy/static smoke: private recipient absent from tracked files; no active iMessage watcher; source diff check clean.

### Real same-account phone release-candidate E2E

- Reused the pinned-compatible installed `imsg 0.15.1` and a User-approved private runtime recipient in an isolated, untracked home.
- The canonical confirmation resolved the GitHub repository identity and the User verified that the phone-visible identity line was structurally `Codex · human-in-loop` without owner, URL, local path, worktree, branch, or task identity.
- Exactly one application `imsg send` mutation occurred. The production send path retained explicit `--service imessage` and `--no-sms-fallback`.
- The iPhone received the release-candidate notification; the User returned the generated token plus the selected option. Strict same-account correlation accepted the exact reply and returned stable choice `looks_correct` through the canonical result path exactly once.
- The live test passed 1/1 in 99.79 seconds. The watcher was killed/reaped and the post-run watcher count was zero; resolved direct-chat identity was persisted only in isolated private configuration.
- After the single E2E, the final failure-path hardening distinguished an unresolved Git repository from a genuine non-repository request. It does not alter the already exercised GitHub-success renderer/send path. The complete deterministic Level-3 suite, production build, install smoke, and static scan were rerun on the final code; no second real message was sent.

## Local temporary state

All task-created Rust/Node caches, isolated install, private runtime configuration, send counter, and wrapper lived under `tmp/HUMAN_IN_LOOP_COMPACT_IMESSAGE_03/`, inherited from the superseded execution. The task-created root `node_modules` symlink was removed. Both macOS Trash attempts timed out on the large scratch tree, so the exact verified task-owned directory was permanently deleted after confirming that no process used it. No User environment, global `imsg`, or global AskHuman installation was changed.

## Acceptance criteria / blockers

All 13 task acceptance criteria are satisfied. Repository-associated confirmations retain the GitHub repository slug as mandatory decision context; non-repository requests may omit it; unresolved repository identity fails before send; compact budgets and canonical choice semantics pass; transport/correlation regressions pass; exactly one real iMessage-only send reached the phone and resolved once; watcher cleanup passed; and private runtime/message data are absent from tracked artifacts and this report. No blocker or design gap remains.

## Git result

- Initial local `HEAD` and tracked upstream both equaled task source `82d0c227e8997e00bac993f4b24c82351aa2b3c8`; its parent equaled baseline `a9d643f7c5dc7cd5d2397ca47a21ba2559a854c0`.
- Implementation commit: `fab135b` (`feat(imessage): require GitHub repository label`).
- Only task-scoped implementation, tests, and this exact report are committed. Pre-existing User state was not modified.
- The report-containing commit is pushed only to `task/260907-imessage-project-label`; Codex verifies local `HEAD`, tracked upstream, and fresh `FETCH_HEAD` equality before returning PASS.
- `master` was not merged. No tag, release, pull request, or repository-visibility change was created.
