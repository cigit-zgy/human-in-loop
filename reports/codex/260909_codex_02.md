---
artifact_type: codex_report
artifact_id: 260909_codex_02
task_id: HUMAN_IN_LOOP_RELEASE_V011_12
title: Publish human-in-loop v0.1.1 hotfix
date: 2026-09-09
project: human-in-loop
repository: cigit-zgy/human-in-loop
status: completed
summary: >
  Integrated the accepted MCP configuration hotfix, qualified the final signed
  installation through one real Codex-MCP iMessage decision round trip, pinned
  the machine-wide Skill, and published the public source-only v0.1.1 release.
verdict: PASS
task_branch: task/260909-release-v0.1.1
task_source_sha: 0c748e6f5b826f7ceeac0201b33b35b9f2c81b3c
baseline_sha: 2ae9fa13f2516c1b2bf73e8b8caa70e34222d1e2
verification_level: level_3
limitations:
  - The release is source-only; no notarized installer or binary asset is published.
  - The repository has no hosted CI workflow, so there was no remote CI run to await.
  - cargo-audit was unavailable in the existing toolchain; targeted Semgrep and the available dependency gate were used.
  - Three repository tests remain intentionally ignored: one live same-account iMessage test, one manual DOCX sample generator, and one optional real Codex CLI probe.
  - Two pre-existing moderate development-only Vitest advisories remain; the high and critical dependency gate is clear.
---

# Publish human-in-loop v0.1.1 hotfix — Codex report

## Release result

```text
VERDICT: PASS
RELEASE_VERSION: v0.1.1
RELEASE_COMMIT: fbd9227ba5fd58d200bc88030847989c034fa506
MASTER_HEAD: fbd9227ba5fd58d200bc88030847989c034fa506 at publication; advanced only by report-only commit(s) afterward
TAG_TARGET: fbd9227ba5fd58d200bc88030847989c034fa506
GITHUB_RELEASE: https://github.com/cigit-zgy/human-in-loop/releases/tag/v0.1.1
README_UPDATED: PASS
VERSION_CONSISTENCY: PASS — all maintained release manifests are 0.1.1
LEGACY_HOME_MIGRATION: PASS — only the known managed isolated HOME is removed
UNKNOWN_CUSTOM_HOME_PRESERVED: PASS
CANONICAL_CONFIG_SHARED: PASS — installed MCP and CLI use the canonical runtime configuration
NO_AVAILABLE_CHANNEL_OBSERVABILITY: PASS — fixed redacted reasons; fail-closed result unchanged
IMESSAGE_HEALTH: ready
MACOS_SETUP: COMPLETE
INSTALLED_MCP_TOOLS: ask_human, notify_human (exactly)
REAL_ASK_HUMAN_QUALIFICATION: PASS — exactly one request, one correlated canonical result
MACHINE_SKILL_PIN: cigit-zgy/human-in-loop@fbd9227ba5fd58d200bc88030847989c034fa506:SKILL.md
TEST_SUMMARY: PASS
SECURITY_SUMMARY: PASS
PRIVACY_SUMMARY: PASS
HUMAN_NOTIFICATION: SENT — one installed-MCP dispatch completed on iMessage; no acknowledgement wait or retry
WORKTREE_CLEANUP: task worktree/scratch scheduled for Git-aware cleanup after final report synchronization; unrelated User worktree preserved
FRESH_FETCH_PROOF: release commit local master == origin/master == FETCH_HEAD before tagging
```

The repository-standard annotated tag object is
`d36382780a40bd784e316e21139def3651057b12`; its peeled target is the release
commit above. Later report-only master commits do not move the tag.

## Changes and integration

ChatGPT supplied and accepted hotfix candidate
`dee7c46edab4404893af48454e939a6645a41dca`. Codex reused that accepted
implementation, added only minimal release-state wording to the root README,
the npm-wrapper README, and `docs/release-notes/v0.1.1.md`, and integrated the
release line into current master with `git merge --ff-only`. Current master had
not advanced from baseline, so no merge conflict, history reconciliation,
reset, rebase, or force push was required.

The released hotfix preserves the accepted design and implementation:

- install/update removes `HUMAN_IN_LOOP_HOME` only when it denotes the known
  project-managed legacy isolated MCP home; unknown User custom homes and all
  unrelated Codex configuration remain untouched;
- new managed Codex MCP entries do not create a second runtime home, so MCP,
  CLI, and daemon share the canonical human-in-loop configuration by default;
- `NoAvailableChannel` remains fail-closed while fixed coarse popup, Feishu,
  and iMessage availability reasons are written through a typed redacted log
  boundary;
- production dedicated-Bot/distinct-Apple-Account topology, explicit
  iMessage-only routing, no-SMS fallback, source-only distribution, and the
  deferred progress-notification scope are unchanged.

No production implementation was changed during release qualification.

## AI-assisted implementation transparency

ChatGPT authored the living-design amendment, formal hotfix task, accepted
candidate, and formal release task. The accepted candidate report records the
Codex-authored migration/observability implementation and tests. In this
release task Codex audited and reused that candidate, authored the bounded
release documentation updates and this report, ran local/macOS evidence, and
performed the explicitly authorized master/tag/GitHub operations. ChatGPT
remains acceptance authority; this report is not self-acceptance.

## Shared coding-Skill alignment

- Collaboration authority:
  `cigit-zgy/agent-collaboration@8601466216515125bf8b17893b2a8e8673bab79e`.
- Shared coding profile: Ponytail `full` at
  `DietrichGebert/ponytail@2ed6c52c9d7e5e56942508591085fd45dea277d3`;
  the installed content hash matched the immutable upstream content hash.
- Project authorities: committed `AGENTS.md`, `SKILL.md`, directly relevant
  `design/05_mcp_interface.md`, `design/06_codex_integration.md`, and
  `design/07_macos_runtime_deployment.md`.
- Release and public-source checks followed the local release/open-sourcing
  Skills subordinate to the committed task. No new feature or dependency was
  introduced.

## Verification evidence

### Candidate and configuration contracts

- Accepted candidate ancestry and baseline ancestry were both proven before
  integration. Release task HEAD was a strict descendant of remote master.
- Focused MCP configuration suite: 47 passed, covering the known managed HOME,
  absolute/tilde/environment spellings, PATH preservation, unknown custom HOME,
  unrelated TOML/comments/order, secondary recognized entry, idempotency,
  ownership, rollback, update detection, and uninstall behavior.
- Focused `NoAvailableChannel` suite: 2 passed. The canonical fallback remains
  `NoAvailableChannel`; log serialization accepts only fixed channel reasons
  and omits recipient/address/chat/GUID/row/message/credential fields.
- Release metadata static tests: 2 passed. All maintained Cargo, Tauri, root
  package, npm wrapper/platform and optional-dependency versions resolve to
  `0.1.1`.
- AskHuman/imsg attribution files are byte-unchanged from `v0.1.0`.

### Final integrated-tree tests and builds

- `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check`: PASS.
- `cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings`:
  PASS.
- `cargo test --manifest-path src-tauri/Cargo.toml`: 1265 discovered, 1262
  passed, 0 failed, 3 ignored. The ignored cases are the explicitly separate
  live same-account iMessage test, a manual DOCX sample emitter, and an optional
  environment-bound real Codex CLI probe. The required real installed-MCP
  distinct-account E2E was run explicitly below.
- `pnpm test`: 28/28 Vitest files and 178/178 tests passed, followed by 23/23
  Node/static tests.
- `pnpm build`: Vue typecheck and production Vite build passed. Vite retained
  its existing advisory for one generated chunk above 500 kB.
- `pnpm tauri build --no-bundle`: PASS; produced an optimized arm64 Mach-O
  reporting `human-in-loop v0.1.1`.
- Rust retained the existing future-compatibility advisory for transitive
  `block 0.1.6`; it is not a current correctness failure.

### Stable production install and no-message health

`scripts/macos-bootstrap.sh` took the prepared routine-update path. It built the
release profile, used the existing stable local signing identity, atomically
installed the coordinator, updated `/Users/Shared` and restarted the Bot worker
without sudo. No administrator/Keychain password, Fast User Switching, TCC
prompt, Apple Messages change, or qualification message was triggered by the
install itself.

- local installed binary: `human-in-loop v0.1.1`, strict codesign valid;
- shared worker binary: `human-in-loop v0.1.1`, strict codesign valid;
- `channel test imessage`: `ready`;
- `macos-setup.mjs status`: all ten current predicates READY/verified and
  `Setup COMPLETE`;
- Codex MCP registration command points to the installed binary, contains no
  managed legacy HOME or isolated-home path, and retains only its explicit PATH
  environment entry;
- installed stdio MCP initialize and `tools/list` passed with exactly
  `ask_human` and `notify_human`; the spawned process environment had no
  `HUMAN_IN_LOOP_HOME` and therefore resolved the same canonical configuration
  path as the installed CLI.

### Exactly-one real installed Codex-MCP qualification

After every deterministic gate passed, the repository protocol client launched
the actual installed binary using the command and PATH registered for Codex MCP.
It performed initialize, verified the exact two-tool public surface, and invoked
one harmless bounded two-choice `ask_human` request for this GitHub repository.

The real path completed:

```text
installed Codex MCP process
→ canonical runtime configuration
→ canonical coordinator and compact renderer
→ dedicated Bot worker
→ explicit iMessage-only delivery
→ iPhone notification
→ one structured human reply
→ strict correlation
→ one canonical MCP result
```

The selected stable choice was `received_correctly`; `source_channel_id` was
`imessage`; a non-empty request id was present. The structured result contained
exactly `request_id`, `selected_choice_id`, and `source_channel_id`. No recipient,
token, chat id/GUID, Messages row, raw message, credential, or other transport
metadata was returned or persisted. One request was sent; no retry occurred.

After completion the daemon reported zero active requests and no iMessage
connections. The task-owned MCP client/process and watcher were reaped. The one
remaining installed MCP process was the pre-existing Codex-app-managed process,
not a task orphan.

### Machine-wide Skill pin

The global activation coordinate now names the immutable release commit:

```text
cigit-zgy/human-in-loop@fbd9227ba5fd58d200bc88030847989c034fa506:SKILL.md
```

The user-scoped discovery file's SHA-256 matches `SKILL.md` fetched from that
exact GitHub revision. Only the owned revision coordinate was changed in the
global policy file; unrelated Codex state was preserved.

### Security, dependencies, privacy, and open-source state

- Final targeted Semgrep `p/security-audit` plus `p/secrets`: 38 rules on all 7
  changed Rust boundary files, approximately 100% parsed lines, 0 findings. An
  initial shell invocation packed newline-separated paths into one argument;
  this verifier invocation defect was corrected and the successful scan rerun.
- `pnpm audit --audit-level high`: PASS with 0 high and 0 critical findings.
  The detailed output retains two moderate advisories in pre-existing
  development-only Vitest tooling. No dependency changed in this hotfix.
- `cargo-audit` was unavailable and was not installed solely for ceremony.
- Manual diff review found no new generic command/file capability, public
  unauthenticated listener, GitHub runtime relay, self-hosted runner, raw
  Messages database surface, credential input, transport-private MCP output,
  SMS/MMS/RCS/carrier fallback, or unnotarized distribution claim.
- The private configured recipient had 0 matches in the tracked tree and 0
  matches in reachable fetched Git history. No private address, message, token,
  chat/GUID/row id, credential, screenshot, or runtime config is in this report,
  release notes, release body, or tracked artifacts.
- Repository visibility is PUBLIC and default branch is `master`. There is no
  hosted CI workflow to await; none was invented. Upstream attribution remains
  complete and unchanged.

## Public release verification

The annotated `v0.1.1` tag was created and pushed normally. GitHub verification
established:

```text
tag: v0.1.1
tag object: d36382780a40bd784e316e21139def3651057b12
peeled target: fbd9227ba5fd58d200bc88030847989c034fa506
release name: human-in-loop v0.1.1
draft: false
prerelease: false
assets: 0
release body matches: docs/release-notes/v0.1.1.md
default branch README: v0.1.1 hotfix state present
```

Only GitHub-generated source archives are published.

## Local temporary state and Git result

All task-created build, protocol-client and linked-worktree state remained under
`<PROJECT_ROOT>/tmp/HUMAN_IN_LOOP_RELEASE_V011_12/`. The release branch was
pushed before integration. Immediately before integration, remote master was
unchanged and the accepted candidate/release task remained a strict descendant;
master fast-forwarded to `fbd9227ba5fd58d200bc88030847989c034fa506`.
Fresh fetch then proved local master, `origin/master`, and `FETCH_HEAD` equal.

After the final report-only push, this task worktree and scratch are removed
with `git worktree remove` and `git worktree prune`. The canonical checkout and
the unrelated `HUMAN_IN_LOOP_LOCAL_CLEANUP_BOT_BOOTSTRAP_07` User worktree are
preserved without reset, stash, rebase, or modification.

## Acceptance and terminal notification

Every blocking acceptance criterion passed: the accepted hotfix and current
design are preserved; v0.1.1 metadata and documentation are consistent; master
integration is non-destructive; deterministic, build, install, security,
privacy, readiness and exact MCP-surface gates are green; the single real
installed-Codex-MCP iMessage decision returned one canonical result; the
machine-wide Skill pin is immutable; and the public source-only non-prerelease
Release is verified.

One compact installed-MCP `notify_human` attempt was made only after the first
durable report commit had been pushed and fresh-fetched. It returned the bounded
canonical dispatch status `SENT` with channel type `imessage`; no acknowledgement
was awaited and no retry was made. This report-only follow-up records that
terminal outcome without moving the release tag.
