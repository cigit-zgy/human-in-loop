---
artifact_type: codex_report
artifact_id: 260907_codex_05
task_id: HUMAN_IN_LOOP_MCP_05
title: Minimal MCP ask_human release verification
date: 2026-09-08
project: human-in-loop
repository: cigit-zgy/human-in-loop
status: completed
summary: >
  Audited and repaired the existing ask_human implementation, completed final-code
  Level-3 regression/build/install/security gates, and verified the installed
  production MCP interface through synthetic recovery tests and one real
  same-account iMessage round trip with exactly one canonical result.
verdict: PASS
task_branch: task/260907-mcp-interface
task_source_sha: 36a4b8e428238ea2ac81148b6873ddf48fb10ec0
baseline_sha: 37c018e47dacf7dce422159d202d2a66d8363add
verification_level: level_3
limitations:
  - Local stdio qualification does not establish remote ChatGPT Pro mutation support.
  - Release verification qualifies this macOS host; other platform builds were not qualified here.
  - Existing dependency-maintenance and local executable-integrity assumptions remain documented below.
---

# Minimal MCP ask_human control interface — Codex report

## Changes

This continues the same formal task and report. Fresh fetch confirmed the existing implementation at `3b7536b21f77e1156155155c96eb4f53379d27e8`; that implementation was audited and reused. The report at that revision is historical evidence, not the fresh verification evidence recorded here. The immutable task, design baseline, branch, and accepted semantics are unchanged.

- The production rmcp stdio router exposes exactly `ask_human`, mapped to existing `ConfirmTask`, `ConfirmEntry`, `ConfirmCoordinator`, configured channels, and canonical `ConfirmResult`.
- Closed input schemas reject unexpected top-level and choice fields. Nonblank semantic choice and caller request IDs are preserved exactly; blank IDs, duplicate choices, invalid recommendation references, and invalid repository paths fail before dispatch.
- Repository identity uses the existing canonical GitHub-origin resolver. Missing/deleted/non-directory/non-repository paths and unresolved or malformed GitHub identities fail closed; an invalid nearest `.git` cannot fall through to an enclosing repository.
- iMessage preparation and finite child processes now honor cancellation, including cancellation during a possibly completed send. No automatic resend was added.
- Daemon channel setup now polls client EOF, deadline, and an already-winning channel while another channel is connecting. Setup cannot hide cancellation or a terminal result.
- Generated MCP/Grok instructions, compaction recovery guidance, and self-call recognition now match `ask_human`. Obsolete tools are not re-exposed. Explicit User approval policy and executable-identity checks remain effective.
- A scoped `cfg(test)` policy-home fixture makes protected-directory tests independent of the scratch location. The production macOS guard is unchanged; the fixture restores thread-local state even after panic and never changes the process environment.
- Added repository-native protocol verification scripts using JSON-RPC over the existing production stdio server and a synthetic external transport. They do not implement a second MCP server or coordinator.

## AI-assisted implementation transparency

ChatGPT authored the accepted living design and committed task. Codex implemented the earlier interface and, in this continuation, audited it before making the bounded repairs above. Separate local reviewers examined contract coverage, generated instructions, process lifecycle, dependency reachability, and security findings.

The implementation reuses rmcp, daemon IPC, the canonical coordinator, and existing channel renderers. It adds no runtime dependency, cloud service, public HTTP listener, GitHub relay, alternative confirmation model, or MCP-specific choice/renderer semantics.

Defects were classified before repair: input/repository/prompt/lifecycle differences from the accepted design were `IMPLEMENTATION_DRIFT`; protected-directory test assumptions were `TEST_DEFECT`. No unresolved `DESIGN_GAP` remained. After the final production changes, the full Rust/lint/frontend/build/install/security gates and installed-artifact protocol verification were rerun.

## Shared coding-Skill alignment

- Collaboration authority: `cigit-zgy/agent-collaboration@8601466216515125bf8b17893b2a8e8673bab79e`.
- Loaded the pinned Skill router and formal, execution, implementation, verification, shared-coding profile, and Codex report owners; local checkout HEAD was not substituted for the pin.
- Activated profile: `ponytail`, `full`, from `DietrichGebert/ponytail@2ed6c52c9d7e5e56942508591085fd45dea277d3:skills/ponytail/SKILL.md`.
- The discovered Skill content matched the exact pinned source. No unrequested upstream refresh or environment migration occurred.
- Project authority was reread in the requested order: `AGENTS.md`, design index, MCP, interaction, coordination, iMessage, Feishu, then the immutable task.

## Verification evidence

### Public MCP surface and schemas

The installed production server completed `initialize`, protocol negotiation, and `tools/list`. Public discovery contained exactly one tool: `ask_human`.

| Contract | Verified behavior |
|---|---|
| Required input fields | `source_agent`, `question`, `choices` |
| Repository input | `repository_path` required for repository-associated decisions; omission only for genuinely non-repository decisions |
| Optional input fields | `context`, `recommended_choice`, `request_id` |
| Choices | 2–6 entries, each with unique stable `id` and compact `label` |
| Unknown fields | Rejected at both request and choice-object boundaries |
| Successful output | Exactly `request_id`, `selected_choice_id`, `source_channel_id` |
| Transport controls | No recipient, chat identity, credentials, imsg arguments, carrier mode, shell/file operation, or raw Messages query capability |

The output carries stable semantic identity, not a renderer's numeric position. No caller-supplied repository display label is accepted. The existing renderer derives the mandatory `Codex · human-in-loop` source/repository line from canonical origin identity.

### Component/property and contract evidence

Focused MCP tests: **13 passed, 0 failed, 0 ignored**, covering schema closure, choice bounds and IDs, canonical mapping, recommendation propagation, request-ID generation/collision handling, output serialization, and private-field exclusion. Protected-directory fixture regressions: **15 passed**; generated integration checks: prompts **18**, context recovery **14**, self-call identity/policy **3**, Grok skill **1**.

Repository tests cover GitHub HTTPS and SSH origins, slug-only display, malformed/non-GitHub origins, invalid nearest repository metadata, symlink-resolved paths, and failure for missing or non-repository paths. A local directory basename is never substituted for unavailable canonical GitHub identity.

Boundary cases cover Unicode question/context/labels, empty values, duplicate IDs, malformed paths, 2/6 valid choices and 0/1/7 rejected choices, valid long input, and existing renderer admission limits. Oversized unsupported iMessage content is rejected rather than truncated into a misleading decision.

Existing iMessage regressions preserve explicit `--service imessage` and `--no-sms-fallback`, bootstrap behavior, compact repository context, collision-safe active tokens, post-send cursor/row boundaries, GUID separation, exact reply grammar, active request state, attachment/reaction rejection, and distinct-peer self rejection.

The expected public tool names, schema fields, stable output identity, and prohibited input fields are asserted independently in the external protocol client. They are not inferred solely from production constants.

### Installed protocol, integration, failure/recovery, and concurrency

Command: `node scripts/mcp-verify.mjs <installed-binary>` against the installed release executable. The final verifier completed 12 categories with 21 invalid payloads, 15 synthetic application sends, 6 canonical results, and **0 real messages**. Every call passed through MCP and the production daemon/coordinator/renderer; only the external imsg transport was synthetic.

| Category | Direct evidence |
|---|---|
| Initialization/discovery | Clean start, negotiation, exact tool/input/output schemas, clean protocol shutdown |
| Negative protocol calls | Missing required fields, empty values, invalid bounds/IDs/recommendation/path, and unknown fields rejected before any send |
| Malformed protocol recovery | Unknown tool/method fail predictably; malformed JSON/request followed by successful ping and later calls |
| Canonical integration | Valid 2/6-choice Unicode calls, recommendation rendered, repository label retained, stable result returned once |
| Strict correlation | Wrong/stale token, bare option, invalid option, wrong chat, sent row/GUID, unrelated reply GUID, reaction, attachment, and outgoing request ignored |
| Failure/recovery | Uncertain send and watcher EOF fail without fake success; the same MCP client successfully executes a later request |
| Explicit cancellation | Request cancellation reaps watcher; a late reply cannot produce success |
| Preparation cancellation | Slow finite preparation is cancelled and reaped; no delayed send occurs |
| In-flight send cancellation | Finite send child is reaped; uncertain mutation is not retried |
| Concurrent pending requests | Distinct active tokens; duplicate active ID rejected without send; cancelling/replying to A cannot resolve B; competing valid candidates produce one winner |
| Disconnect/server termination | EOF and SIGTERM with two pending requests each leave zero owned requests/watchers |
| Repeat cleanup | All verifier clients/servers stopped; registry and owned process checks repeated |

The coordinator's component tests independently preserve first-terminal-winner behavior and reject duplicate/late candidates. Feishu/coordinator regressions ran in the full suite. A paused-time daemon regression separately exercises disconnect, expiry, and another channel's winner while setup remains pending; the setup future is dropped in all three cases.

### Full final-state repository regression

| Command/gate | Final result |
|---|---|
| `cargo test --manifest-path src-tauri/Cargo.toml` | **1222 passed, 0 failed, 3 ignored** |
| `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` | PASS |
| `cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings` | PASS |
| `pnpm test` | Vitest: **28 files / 178 tests PASS**; Node/static: **9 PASS** |
| `pnpm build` | Typecheck and production Vite build PASS |
| `pnpm tauri build --no-bundle` | Optimized production executable built successfully |

The three ignored Rust tests are explicit and do not substitute for this task's E2E:

1. The direct internal same-account hardware probe requires a private recipient and reply. The required fresh live test instead ran through the installed MCP protocol, below.
2. `real_codex_fork_help_when_available` probes an optional installed Codex fork CLI; unrelated to the maintained MCP contract.
3. `emit_manual_samples` writes legacy DingTalk document samples for visual inspection; that channel is not maintained and the sample generator is unrelated to this task.

The reused Rust toolchain was cargo/rustc 1.98.1; Node 26.8.1 and pnpm 11.25.0 were used. Node required a task-local local-storage file for frontend tests. Existing transitive `block 0.1.6` future-incompatibility and Vite large-chunk advisories remained. pnpm also warned that the old package-level `onlyBuiltDependencies` setting is no longer read. These were nonfatal notices, not hidden test/lint/build/install failures; no unrelated dependency/config migration was made.

### Production installation and self-containment

`scripts/install.sh --release` installed the final release candidate into an isolated task-local installation, using the repository's normal installation path. The installed executable reported `AskHuman v0.13.1`; `codesign --verify --strict` passed for its ad-hoc local signature. Maintained channel help remained limited to Feishu and iMessage.

Installed MCP discovery and negative validation proved that the packaged executable launches the production handler and rejects an invalid one-choice call before message dispatch. Its tool schema matched the development contract.

For the final 12-category installed-protocol run, the original Cargo build tree was temporarily moved aside and then restored. The installed binary still completed all categories, establishing that it did not depend on that worktree build output. `otool -L` showed system frameworks/libraries and standard Swift runtime dependencies, not task build-tree libraries. Application configuration remained isolated, and `imsg 0.15.1` remained a separate installed dependency.

### Static/security and dependency evidence

Semgrep `1.176.0`, official `p/rust`, scanned the final 19 relevant Rust files with 11 rules: approximately 100% parsed, 0 scan errors, 12 INFO audit findings. Coverage included all MCP modules, coordinator, daemon dispatch/request, client/IPC, repository identity, Feishu/iMessage boundaries, MCP integration/prompt/self-call logic, and permission-diff safety/worker files.

All findings were manually classified:

- Ten `current_exe` occurrences: existing daemon handshake/configuration, fixed self/helper/worker launches, legacy non-routed MCP code, and self-call identity checks. No new MCP input selects an executable or command. Local installed-executable integrity remains an assumption; path equality is not cryptographic binary authentication.
- One `unsafe` occurrence is test-only `libc::kill(pid, 0)` process-liveness inspection; it sends no signal.
- One `temp_dir` occurrence is existing aged `askhuman` temporary-cache cleanup, outside caller-controlled MCP inputs; this task did not broaden its deletion behavior.

Manual boundary review found no public shell/file/credential/database endpoint, unauthenticated HTTP listener, GitHub relay workflow, self-hosted runner, or carrier fallback. The new handler does not log private transport input/results. Existing Feishu local diagnostic error text is not universally redacted; it is not returned by the new MCP canonical fallback path, and no credential leak was established in the inspected asynchronous connection path.

`cargo-audit 0.22.2` checked 634 lockfile dependencies against advisory database revision `8a1eb4f933fb5821add5b4e98601ebd90b8b3538` (updated 2026-09-07): 0 advisory vulnerabilities; 17 unmaintained, 2 unsound, and 1 yanked informational entries.

- Five unmaintained `unic-* 0.9.0` crates are reachable through `urlpattern → tauri-utils`; this is an existing maintenance risk, without a vulnerability reported by this audit.
- The remaining 12 unmaintained dependencies are absent from the current macOS graph.
- `event-listener 5.4.1` (`RUSTSEC-2026-0221`) and `glib 0.18.5` (`RUSTSEC-2024-0429`) are absent from the `aarch64-apple-darwin` graph.
- Yanked `chacha20 0.10.1` is also absent. Its lockfile path passes through optional HTTP3 dependencies; current reqwest features do not enable HTTP3.

Reachability used `cargo tree --locked --offline --manifest-path src-tauri/Cargo.toml --target aarch64-apple-darwin`. All-target offline enumeration lacked an uncached dependency, so no all-platform security qualification is claimed. No dependency upgrade was made to suppress findings.

### Fresh real installed MCP → same-account iMessage E2E

All non-message gates above passed on the final production code before the live call. A real local protocol client initialized the installed server, negotiated protocol `2025-11-25`, and invoked production `tools/call ask_human` using the canonical repository root, source `Codex`, compact release-candidate context, and the task's two stable choices. The approved local configuration stayed private.

The User was instructed to check the required source/repository line on the iPhone and reply there using the generated token and option. The affirmative exact reply returned through same-account synchronization. This establishes the requested phone round trip with the User's confirmation; Messages metadata does not cryptographically identify the originating physical device.

| Live chain evidence | Result |
|---|---|
| Real client → initialize → `tools/call` → validated canonical request | PASS; one canonical request |
| Existing coordinator → production renderer → resolved repository | PASS; phone-visible `Codex · human-in-loop` |
| Application send mutation | **Exactly 1** |
| Explicit iMessage service / SMS fallback disabled | PASS / PASS |
| iPhone receipt and exact User reply | Affirmative same-account reply accepted |
| Bounded watcher/history → strict correlation | PASS; production post-send row/cursor/GUID/token/active-state checks retained |
| Canonical result → MCP tool response | `selected_choice_id = received_correctly`; `source_channel_id = imessage`; request ID present |
| Terminal tool results | **Exactly 1**; duplicate acceptance blocked by the coordinator |
| Private transport output fields / stderr bytes | **0 / 0** |
| Active requests / owned imsg children after completion | **0 / 0** |
| Owned MCP server, daemon, and client cleanup | PASS; checks repeated twice |

The live call was not replaced by an internal Rust function, synthetic reply, direct coordinator invocation, or direct imsg invocation. No extra real request or blind retry occurred. The 15 automated synthetic sends above are distinct from this single real mutation.

### Privacy and open-source readiness

The tracked candidate scan found 0 private-recipient matches. No private configuration, authentication material, token, chat identity, Messages row/content, screenshot, runtime log, scratch tree, or build output is committed. The report records only structural counts and the public canonical source/repository and choice/channel labels.

Production documentation/examples contain no personal absolute paths. README and generated MCP instructions match the actual tool surface. Upstream AskHuman MIT attribution remains in `LICENSES/AskHuman-MIT.txt` and `THIRD_PARTY.md`; imsg source is not vendored. No documentation claims that this local task connects the current ChatGPT Pro conversation to a remotely callable mutation tool.

## Local temporary state

All newly created scratch, isolated configuration, installation, protocol clients, synthetic transport, scan output, and live structural evidence stayed under `tmp/HUMAN_IN_LOOP_MCP_05/`. Existing approved configuration and healthy toolchain/dependency caches were reused without changing their ownership.

Current-task requests, MCP/daemon/client processes, and imsg children are stopped; repeated owned-process checks passed. Two pre-existing application/daemon processes outside this execution's ownership were preserved. No claim is made that every AskHuman process on the Mac was stopped.

After extracting the structural evidence into this report and confirming zero current-task processes, the entire task-created scratch tree and the temporary frontend dependency symlink were removed. Generated verification state can be reconstructed from the maintained scripts; private runtime copies and logs were not retained as repository artifacts. The previously approved source configuration and existing dependency caches were preserved. No linked worktree existed; the final worktree inventory contains only the canonical checkout.

## Acceptance criteria / blockers

The implementation, deterministic tests, final full regression, production build/install, installed MCP protocol/failure/recovery checks, security/privacy audit, one fresh real MCP/iMessage round trip, temporary-state cleanup, and Git closure satisfy the task's execution criteria. No unresolved design gap or runtime prerequisite remains.

Known limits are local stdio/macOS qualification, the documented dependency-maintenance and installed-executable trust assumptions, and generic-tool presentation of new MCP calls in the legacy transcript UI. Remote authenticated deployment, ChatGPT product support, and transcript-card redesign are separate concerns.

Codex's verdict is execution evidence only. ChatGPT acceptance review remains pending, and post-acceptance integration retains the User checkpoint.

## Git result

Initial fresh `git fetch --all --prune` and branch/HEAD/upstream/status/worktree inspection confirmed `task/260907-mcp-interface` at `3b7536b21f77e1156155155c96eb4f53379d27e8`, including normal ancestry from task commit `36a4b8e428238ea2ac81148b6873ddf48fb10ec0`. Existing correct implementation and User-local state were preserved; no reset, stash, rebase, cherry-pick, or force-push was used.

Task-scoped source, tests, documentation and this report are delivered in a Conventional Commit on the existing task branch. The report-containing commit is identified by the immutable completion link. Post-push closure is verified by a fresh fetch: local `HEAD == @{upstream} == FETCH_HEAD`, clean `git status --short`, and the immutable task commit still an ancestor. No unrelated User-local changes are included.

No new task or feature branch was created. `master` was not merged; no tag/release was created; repository visibility was not changed.
