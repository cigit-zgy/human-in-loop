# HUMAN_IN_LOOP_RELEASE_V013_21 — v0.1.3 release evidence

## Result

**VERDICT: PASS.** Release/install/pin/publication gates passed, and the single
terminal notification returned SENT after this report's first commit was pushed
and fresh-fetched. Its receipt is recorded below.

ChatGPT acceptance was supplied as PASS by the committed release task. This
report executes that accepted release; it does not substitute Codex self-review
for ChatGPT acceptance or reopen product design.

## Authority and exact identities

| Field | Evidence |
| --- | --- |
| TASK_SOURCE | `reports/chatgpt/260910_chatgpt_06.md@bd2be3c82116bf4c4d2433ef22cfacd2285d7aea` |
| TASK_BRANCH | `task/260910-release-v0.1.3` |
| CHATGPT_ACCEPTANCE | PASS, explicitly supplied in the committed task and User instruction |
| ACCEPTED_IMPLEMENTATION_COMMIT | `7dd8782823037ed944d9bfd031b85493695c47bd` |
| ACCEPTED_REPORT_COMMIT | `a6f738ef73a6a6e70187a27979710f23e425add3`, `reports/codex/260910_codex_05.md` |
| COLLABORATION | `cigit-zgy/agent-collaboration@8601466216515125bf8b17893b2a8e8673bab79e` |
| RELEASE_COMMIT | `3e4137dcd4f89950a9c284634e089ccecffcd0ce` |
| MASTER_HEAD_BEFORE_RELEASE | `779c244b2137c33f2b5cd4afe2644f0fdd0587c0` |
| MASTER_HEAD_AFTER_RELEASE | `3e4137dcd4f89950a9c284634e089ccecffcd0ce` |
| TAG | `v0.1.3`, annotated Git tag |
| TAG_OBJECT | `37ce790d34696c4cf61a054704f0f7b9a52f112d` |
| TAG_TARGET | `3e4137dcd4f89950a9c284634e089ccecffcd0ce` |
| GITHUB_RELEASE | https://github.com/cigit-zgy/human-in-loop/releases/tag/v0.1.3 |
| RELEASE_ASSETS | `[]`; public, draft=false, prerelease=false; source-only |
| PUBLISHED_AT | `2026-09-10T09:32:52Z` |
| REPORT_PATH | `reports/codex/260910_codex_06.md`; requested number was free |

`master` was integrated by fast-forward, retaining the accepted implementation,
design, tasks and reports. Subsequent report-only commits remain on the release
task branch; master and the tag remain at RELEASE_COMMIT. GitHub's
`targetCommitish=master` metadata does not determine an existing tag's target:
both local peel and remote `refs/tags/v0.1.3^{}` independently equal RELEASE_COMMIT.

## Release content and evidence reuse

The release-content commit changes twelve paths: Cargo package version and lock
entry, Tauri version, root package version, maintained npm wrapper/platform
version declarations, README, wrapper README, and v0.1.3 release notes. Version
surfaces are 0.1.3. No npm/crates publication or binary asset upload occurred.

The final tree has an empty diff against accepted implementation `7dd8782...`
for `src-tauri/src/`, `src-tauri/build.rs`, `src/`, `scripts/`, `design/`, `SKILL.md`,
and `pnpm-lock.yaml`. Manifest diffs are version substitutions only; Cargo.lock
changes only this product's package version, without dependency changes.
Historical provenance and unreachable GUI/updater source were not redesigned.

The accepted real phone evidence is therefore reused legitimately:

```text
REAL_SEND_COUNT=1                    # accepted implementation qualification
REAL_RESULT_COUNT=1                  # accepted implementation qualification
REAL_SELECTED_CHOICE_ID=received_correctly
REAL_SOURCE_CHANNEL_ID=imessage
RELEASE_TASK_NEW_REAL_ASK_COUNT=0
ACCEPTED_CLIENT_EXIT=0
ACCEPTED_DAEMON_IDLE=true
ACCEPTED_IMSG_PROCESSES=0
```

The accepted report proves watcher/request/client cleanup. This release does
not claim to have observed whether that real reply won via watch or catch-up;
that distinction was not instrumented in the accepted live result. The release
adds no new real decision request merely to repeat accepted evidence.

## Final release-tree gates

**TEST_SUMMARY:** all required final gates passed, including both release-tree
and installed-binary production MCP synthetic verification.

| Gate | Result |
| --- | --- |
| `cargo test --manifest-path src-tauri/Cargo.toml` | 1227 passed, 0 failed, 3 ignored |
| `cargo fmt --manifest-path src-tauri/Cargo.toml --check` | PASS |
| `cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings` | PASS |
| `pnpm test` | Vitest: 178 passed / 28 files; Node: 32 passed |
| `pnpm build` | PASS: type-check and production frontend build |
| `pnpm tauri build --no-bundle` | PASS: optimized 0.1.3 binary |
| Skill `quick_validate.py` | PASS: Skill is valid |
| Release-tree production MCP synthetic verifier | 22 checks PASS; 32 synthetic sends; 8 canonical synthetic results; 0 real sends |
| Exact installed-binary production MCP synthetic verifier | Same 22 checks PASS; 32 synthetic sends; 8 canonical synthetic results; 0 real sends |
| Synthetic cleanup, both runs | active_requests=0; synthetic_processes_alive=0; daemon_and_mcp_cleanup=true |
| `git diff --check` / staged diff check | PASS |
| Runtime-source and dependency equivalence | PASS as described above |

The three Rust ignores are the existing environment-bound real same-account
round trip, manual document-sample emitter, and Codex fork-help check. The
separate real Codex CLI contract test passed. No new ignore or test relaxation
was introduced. The compiler reports the inherited future-compatibility warning
for `block v0.1.6`; frontend build reports its existing chunk-size warning.
Neither is a gate failure. No GitHub Actions workflows are configured in this
tree; no remote CI success is claimed.

Initial release prose explicitly named the retired enterprise channel, which
failed the existing current-documentation absence assertion. The prose was
corrected to describe its removal without reintroducing the retired name into
current product docs. Final `pnpm test` passed unchanged. Two available Python
interpreters lacked PyYAML; the existing Miniforge interpreter with PyYAML 6.0.3
ran the maintained Skill validator. No validation dependency was installed.

## Installed runtime and production invariants

The clean RELEASE_COMMIT was rebuilt and installed by
`scripts/install.sh --global --release` with this task's isolated Cargo target.
The maintained installer performed stable signing and atomic coordinator
replacement. Its normal dependency step reused the copied healthy dependency
state and left the tracked lockfile unchanged. The prepared-runtime predicate
and routine atomic Shared-binary update from `scripts/macos-bootstrap.sh` were
used without invoking one-time bootstrap, administrator fallback, qualification,
or optional-autologin onboarding. Worker restart returned `worker_restarting`,
then `ready`. The Shared requirement already matched and was preserved.

| Field | Evidence |
| --- | --- |
| INSTALLED_VERSION | `human-in-loop v0.1.3`, both installed paths |
| INSTALLED_BINARY_MATCH | Primary and Shared SHA-256 both `32dc11692531bfbd489e5ec20ae9f602f5f4778f9488212cc0687674af13efce` |
| INSTALL_SOURCE_HASH | Maintained install receipt: `d3d504a2b748455eb6a57672d8026e626edb7d181a77f94f6e3e7aa191006d30` before installer signing |
| LEGACY_TRAY_STATE | No legacy LaunchAgent file, loaded GUI job, or running GUI host; explicit `--gui-host` exits 1 with retired/headless diagnostic |
| LEGACY_UPDATER_STATE | `update prepare` exits 1 with retired-updater diagnostic; production updater rejection regression PASS |
| MAINTAINED_CHANNELS | Exactly `[imessage]`; installed `channel list` contains one enabled/configured channel |
| FEISHU_REMOVAL_REGRESSION | Runtime/config/keyring/source regressions PASS; no compiled maintained capability; obsolete on-disk key removed as detailed below |
| MCP_SURFACE | Actual installed stdio initialize/list PASS; exactly `ask_human`, `notify_human`; probe client exited 0 |
| DAEMON_STARTUP | Unattended `daemon start` PASS; version 0.1.3, protocol 2, zero active requests |
| BOT_WORKER_HEALTH | ready; new process at Shared binary under Bot UID 502 |
| IMESSAGE_HEALTH | `channel test imessage` = ready |
| SETUP_STATE | Setup COMPLETE before and after install and configuration cleanup |
| NUMERIC_TOKEN_REGRESSION | PASS: decimal TOKEN-OPTION, collision, malformed/stale/invalid reply rejection |
| HISTORY_CATCHUP_REGRESSION | PASS: silent watch recovery, full correlation rejection, cancellation, duplicate/late resolution and child cleanup |
| NO_SMS_REGRESSION | PASS: explicit iMessage and `--no-sms-fallback`; no carrier route introduced |
| RICH_BODY_REGRESSION | PASS: real MCP/daemon/production renderer with synthetic multiline review body |
| LINK_PRESENTATION_REGRESSION | PASS: quoted locator byte preservation; accepted no-large-preview/tappable phone proof reused |
| AUTOLOGIN_REGRESSION | PASS: existing optional-autologin tests; host preference unchanged |
| HOME_MIGRATION_REGRESSION | PASS: canonical-HOME Rust regression; installed daemon socket in primary canonical HOME; Codex registration has no override |
| SKILL_PIN | `cigit-zgy/human-in-loop@3e4137dcd4f89950a9c284634e089ccecffcd0ce:SKILL.md` |
| SKILL_DISCOVERY_COPY_MATCH | Exact Git blob bytes; SHA-256 `1b1ffd105c13f44031921033043e9a1a58eccbc050d61acca05bbedb6d9c7c2b` |

Both installed binaries pass `codesign --verify --strict` and satisfy the
unchanged designated requirement:

```text
identifier "io.github.cigit-zgy.human-in-loop" and certificate root = H"710a013e00864b3c431d4a2167e48474829c2b08"
```

This is the existing machine-local signing identity, not a notarization claim.
No SecurityAgent process or signing/Keychain consent prompt appeared during the
installed checks. Unrelated existing MCP clients were preserved.

A post-install inspection found an inert `channels.feishu` key retained in the
old on-disk JSON. The accepted `ChannelsConfig` no longer has that field, and
installed `channel list` already exposed only iMessage. The obsolete key alone
was removed atomically; structural comparison proves every other value,
including the complete iMessage recipient/identity configuration, unchanged.
This is a mechanical stale-config cleanup, not a runtime or transport change.
Old Keychain credentials were not read or deleted. Setup COMPLETE was rechecked.

Machine-wide policy replacement changed exactly the old human-in-loop commit
pin in `~/.codex/AGENTS.md`; every unrelated policy byte was preserved. The
existing discovery copy was replaced with the exact RELEASE_COMMIT Skill blob.
Codex MCP configuration was verified read-only: primary installed binary,
`["mcp"]`, no `HUMAN_IN_LOOP_HOME` override.

## Security, privacy and synchronization

**SECURITY_SUMMARY:** version/documentation-only source diff, unchanged runtime
and dependencies, stable requirement enforcement on both installed binaries,
existing channel-scope/no-carrier tests, strict MCP correlation and lifecycle
regressions all PASS. No new security-sensitive implementation warrants a new
scanner/dependency-audit campaign. Existing license/provenance is preserved;
repository was already public. No force-push, squash, security-setting change,
TCC reset, Apple Account operation, Bot recreation, recipient change, reboot,
FileVault/SIP change, private IMCore, or direct Messages DB access occurred.

**PRIVACY_SUMMARY:** release notes and this report contain structural evidence
only: no recipient, chat identity, live reply token, message text, credential,
private key, or runtime config content. Synthetic fixtures remain isolated from
the real imsg executable and user configuration. No live history query or new
phone decision was required by this release.

**FRESH_FETCH_PROOF:** after FF integration and atomic push, fresh fetch returned:

```text
local master = origin/master
             = task/260910-release-v0.1.3
             = origin/task/260910-release-v0.1.3
             = 3e4137dcd4f89950a9c284634e089ccecffcd0ce
```

The first push encountered a transient TLS connection failure before updating
remote refs. A fresh fetch confirmed unchanged remote heads; the safe atomic
push then succeeded, followed by the equality proof above. The annotated tag
was independently read back from the remote, and the published Release API
returned the expected public source-only metadata and release-note bytes.

This report is committed and pushed on the task branch after publication, then
fresh-fetched with local task HEAD required to equal its upstream. The final
response supplies the immutable report commit. No report commit retargets the
tag or changes the machine Skill pin.

Local retained evidence is under `tmp/HUMAN_IN_LOOP_RELEASE_V013_21/`: test/build
logs, `m/result.json`, `n/result.json`, source equivalence, setup and installed
MCP/surface/config-migration receipts, Release API metadata, and rollback
runtime/policy copies. No unrelated worktree or user state was removed.

## Terminal notification

**HUMAN_NOTIFICATION: SENT.** Exactly one terminal `notify_human` attempt was
made after the initial report commit
`63bb00de2beef6a0ba481c2f2e8b1bbfcab7bbf8` was pushed and fresh-fetched with
local task HEAD equal to its upstream. Its immutable report URL was included
in the same application message.

```json
{
  "channel_ids": ["imessage"],
  "delivery_status": "SENT",
  "notification_id": "HUMAN_IN_LOOP_RELEASE_V013_21-terminal"
}
```

The tool returned `isError=false`. Attempt count is one; routine progress and
new real `ask_human` counts are zero. No acknowledgement was requested or
awaited. This report-only receipt commit is pushed and fresh-fetched without a
second notification; RELEASE_COMMIT, master, tag and Skill pin stay unchanged.
