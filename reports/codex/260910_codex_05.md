---
artifact_type: codex_report
artifact_id: 260910_codex_05
task_id: HUMAN_IN_LOOP_HEADLESS_INBOUND_SYNC_20
title: Headless production retirement and bounded iMessage catch-up qualification
date: 2026-09-10
project: human-in-loop
repository: cigit-zgy/human-in-loop
status: completed
verdict: PASS
task_branch: task/260910-headless-inbound-sync-v0.1.3
task_source_sha: f40e4829c8060369c3ad5818337a7ac0ed34773b
baseline_sha: b8f9dba735dae95772bed2e159c98d6312c4072e
verification_level: level_3
summary: >
  Removed the positively identified legacy GUI login launch, disabled production
  GUI/updater entry points, added redacted Bot-owned history diagnosis and bounded
  catch-up using the original correlation ledger. One fresh installed-MCP request
  returned received_correctly through imessage and completed process cleanup.
limitations:
  - The old reply's arrival relative to the previous cancellation cannot be reconstructed.
  - The fresh winner was not instrumented to distinguish watch from history; both share the same resolver.
  - Historical GUI/updater source remains unreachable from maintained production launch and update selection.
  - This is Codex execution PASS; ChatGPT acceptance and subsequent release gates remain separate.
---

# Headless recovery and live qualification — Codex report

## Result

`PASS` for this execution. This is not ChatGPT acceptance and does not publish
v0.1.3. The requested `260910_codex_04.md` was occupied by the prior diagnostic
stop at `e939083a2409b2c8a6ccd194e620f14241135d30`; this report uses the next free
number and preserves that historical report unchanged.

Implementation commit:
`7dd8782823037ed944d9bfd031b85493695c47bd`.

The real installed-MCP response was:

```json
{
  "request_id": "5cf76583-779b-4872-a280-53e0d93317c1",
  "selected_choice_id": "received_correctly",
  "source_channel_id": "imessage"
}
```

This object came from `response.result.structuredContent`, not from a local
transcription or inferred phone answer. Exactly one response matched the call.

## Required result fields

| Field | Evidence |
| --- | --- |
| VERDICT | PASS — Codex execution only |
| TASK_SOURCE | `reports/chatgpt/260910_chatgpt_05.md` at `f40e4829c8060369c3ad5818337a7ac0ed34773b` |
| INHERITED_BLOCKED_REPORT | `reports/codex/260910_codex_03.md` at `b8f9dba735dae95772bed2e159c98d6312c4072e`; prior attempt report 04 also preserved |
| LEGACY_TRAY_PROCESS_IDENTIFIED | YES: primary-user PID 96594, exact executable and LaunchAgent below |
| LEGACY_TRAY_REQUIRED_FOR_RUNTIME | NO: distinct GUI role; daemon, installed MCP and Bot worker qualified after its removal |
| LEGACY_TRAY_REMOVED | YES: exact loaded service booted out; zero GUI host processes after headless startup and real qualification |
| LEGACY_GUI_AUTOSTART_REMOVED | YES: exact GUI plist moved out of LaunchAgents; production install functions reject GUI installation; daemon spawn hooks removed |
| LEGACY_UPDATER_REMOVED | Production selection, periodic remote check and CLI prepare retired; cached version offers suppressed |
| ASKHUMAN_013X_UPDATE_PATH_REMOVED | YES in production; historical upstream implementations cannot be selected, and cached 0.13.1 cannot become an offer |
| HEADLESS_DAEMON_STARTUP | PROVEN_LIVE: unattended start, PID 9790, no tray host, zero active requests after qualification |
| MCP_SURFACE | Installed initialize/tools-list: exactly `ask_human` and `notify_human` |
| BOT_WORKER_HEALTH | ready; dedicated Bot session READY |
| IMESSAGE_HEALTH | ready; Automation READY; setup status COMPLETE after qualification |
| OLD_DELIVERED_REPLY_CLASSIFICATION | HISTORY_ROW_PRESENT_WATCH_MISSED; one uniquely identified old outgoing record and one compatible post-send inbound reply |
| ROOT_CAUSE_CLASSIFICATION | Old reply now exists in Bot history. Missing history reconciliation was boundedly repaired; no current storage-absence diagnosis is claimed. |
| WATCHER_RECONCILIATION_CHANGED | YES: shared resolver, at most 60 scans of 200 records, five-second interval, original request deadline |
| MESSAGES_SESSION_RECOVERY_CHANGED | NO account/Messages-service recovery; worker restarted only to load the stable-signed implementation |
| INBOUND_SYNC_DIAGNOSTIC_STATE | Explicit redacted history classification added. No persistent storage-stall flag: a present old reply and successful fresh round trip do not establish a current storage stall. |
| REAL_ASK_HUMAN_QUALIFICATION | PROVEN_LIVE: installed binary from the Codex registration, canonical HOME, one genuine phone reply |
| REAL_SEND_COUNT | 1 qualification send, independently confirmed by one matching Bot-owned outgoing record |
| REAL_RESULT_COUNT | 1 canonical result; one compatible Bot-owned post-send inbound record |
| REAL_SELECTED_CHOICE_ID | received_correctly |
| REAL_SOURCE_CHANNEL_ID | imessage |
| NUMERIC_TOKEN_REGRESSION | Decimal renderer/parser/ledger tests and genuine exact-format reply; real token omitted |
| LINK_PRESENTATION_REGRESSION | Same quoted immutable task URL in the one multiline message; inherited no-large-preview/tappable presentation unchanged; no separate link probe |
| FEISHU_REMOVAL_REGRESSION | Node current-source/channel audit PASS; no Feishu restored |
| NO_SMS_REGRESSION | Explicit iMessage plus no-SMS send checks PASS; no fallback, no resend |
| RICH_BODY_REGRESSION | 600–800-character synthetic multiline MCP test PASS; real multiline synthetic card accepted by User choice |
| AUTOLOGIN_REGRESSION | All existing optional-autologin Node tests PASS; no autologin setting changed |
| HOME_MIGRATION_REGRESSION | Rust/runtime-identity checks PASS; Codex registration has no HUMAN_IN_LOOP_HOME override; canonical signing/TCC remains ready |
| TERMINAL_NOTIFY_REGRESSION | Default-zero progress and bounded terminal regressions PASS; this resumed execution makes at most one terminal notification attempt, after report publication |
| TEST_SUMMARY | Rust 1227 passed / 3 ignored; Vitest 178 passed; Node 32 passed; installed-binary synthetic MCP 22 checks PASS; fmt, Clippy and optimized build PASS |
| SECURITY_SUMMARY | Narrow authenticated worker operation; no generic command/file/database access. Semgrep: 21 existing FFI/current_exe review findings, zero parse errors, no newly introduced unsafe block. |
| PRIVACY_SUMMARY | No real recipient/sender/token/chat/GUID/row/message/credential/Keychain contents in this report or task artifacts |
| WORKTREE_CLEANUP | Real request/client/watchers cleaned; production daemon and worker intentionally retained. Task build/rollback evidence retained for acceptance and the authorized release follow-up. |
| FRESH_FETCH_PROOF | Implementation HEAD = tracked upstream = FETCH_HEAD = `7dd8782823037ed944d9bfd031b85493695c47bd`; report publication equality follows below |
| HUMAN_NOTIFICATION | One attempt after the report commit/push/fresh-fetch; terminal tool output records actual delivery outcome |

## Positive GUI ownership and retirement

The observed tray was not an arbitrary `/Applications` app. It was the signed
standalone Mach-O `/Users/wenv/.local/bin/human-in-loop --gui-host`, owned by
`wenv`, PID 96594, launched by launchd from:

`/Users/wenv/Library/LaunchAgents/com.naituw.humaninloop.guihost.plist`.

The loaded label was `com.naituw.humaninloop.guihost`, with RunAtLoad and KeepAlive
enabled and ProcessType Interactive. The executable's code identifier was
`io.github.cigit-zgy.human-in-loop`, signing authority
`human-in-loop Local Code Signing`, with no TeamIdentifier. The initial Bot
worker was a different process, PID 54238, under the dedicated Bot account,
running `/Users/Shared/human-in-loop/bin/human-in-loop imessage-worker run`.

Source identified the unwanted update authority as `Naituw/AskHuman`, npm
`askhuman`, and its upstream signing identity. The live cache contained 0.13.1.
This was distinct from the maintained product's signing and release authority.

After a zero-active-request/no-watcher check, Codex booted out only that exact
GUI service and moved only its positively identified plist into the task-local
rollback directory. It preserved the shared executable responsibility,
daemon LaunchAgent, MCP registration, Bot LaunchAgent, imsg, canonical data and
TCC identity. No arbitrary application, login item or user data was removed.

Implementation removes daemon GUI spawn hooks and periodic upstream checks,
rejects the GUI launch/spawn and GUI login-install paths, makes legacy menu-bar
configuration inert and non-serializing, disables updater selection and CLI
prepare, and prevents cached upstream versions from resurfacing as offers.
Both explicit `--gui-host` and `update prepare` smoke checks reject operation.

Historical GUI/frontend/updater implementation and internal types remain for
source provenance and existing non-production references; this was not a
frontend rewrite. They are not a maintained production GUI, autostart or updater.
The upstream version/package/signing constants are not used by the production
updater selection. Existing binary-change detection for daemon lifecycle remains.

## Old-reply evidence and bounded repair

The new `imessage-worker diagnose-history <immutable-task-url>` operation accepts
only a bounded immutable GitHub task locator. Existing kernel peer checks and
configured-recipient validation run before it. It cannot select a different chat,
command, file or database. The Bot-owned implementation uses the existing public
basic imsg history reader, with a 200-record cap and finite subprocess timeout.
Only classification and counts cross back to the coordinator.

Before any new message, the old task locator uniquely identified an outgoing
structured request, and its exact-format post-send peer reply was present.
The diagnostic did not register a pending request, submit an answer, or mutate
the old cancellation. This removed the previous report's evidence blocker.

The task-defined `HISTORY_ROW_PRESENT_WATCH_MISSED` label describes the old
cancelled, unanswered incident. History alone cannot establish whether its row
arrived while the old watcher was active or after cancellation. Likewise,
row-presence counters on the successful new request do not prove its watcher
missed anything. No Apple synchronization-service root cause is invented.

Both dedicated-worker and local confirmation paths now call one
`wait_correlated_reply` implementation. Its live and history candidates pass
through the original `PendingReplies::resolve`, including request deadline,
terminal winner, configured chat, post-send row, direction, numeric token,
option ledger, GUID/reply-to, reaction and attachment checks. The live resolver
itself was not loosened. History candidates are evaluated oldest-first within
the bounded snapshot, never by accepting an arbitrary last message.

The timer performs at most 60 history scans five seconds apart and never
extends the request deadline. A cancellation-safe line reader preserves partial
NDJSON across timer selection. Caller cancellation drops the pending operation;
both callers then kill and reap their watcher. Finite history children retain
kill-on-drop behavior. After the catch-up budget, live watch remains subject to
the original deadline; there is no unbounded history polling or automatic resend.

## Verification and real qualification

PROVEN_DETERMINISTIC:

- Full Rust tests: 1227 passed, zero failed, three pre-existing ignored tests
  (private same-account phone test, sample emitter, environment-dependent Codex
  fork-help test). The distinct-Bot real path was qualified separately below.
- Vitest: 178 tests in 28 files. Node: 32 checks, including Feishu absence,
  no-SMS, bootstrap, canonical runtime identity and optional autologin.
- `cargo fmt --check`, all-target Clippy with `-D warnings`, local-install build
  and optimized release build passed. An inherited `block` dependency emits a
  future Rust incompatibility warning; no dependency change was made.
- Final installed-binary MCP harness: 22 checks, 32 synthetic sends, eight
  canonical synthetic answers, zero real sends. New cases force a silent watcher,
  reject incorrect history candidates, recover one valid reply, reject duplicates,
  cancel catch-up and prove a late reply cannot resurrect a request. All clients,
  synthetic children and harness daemons are reaped.
- Semgrep ran 11 Rust rules on five affected boundary files. Its 21 findings
  concern retained FFI and current-executable calls; no new unsafe blocks or
  privilege boundary were added. Existing calls remain fixed-target AppleEvents,
  kernel credential/UID checks and platform login metadata, not arbitrary
  command execution. No zero-finding or broader dependency-audit claim is made.

Build/test corrections were completed before the real send: copied Swift module
caches needed regeneration because they embed their old absolute path; the first
MCP scratch path exceeded the Unix socket path limit; the two added synthetic
calls required updated aggregate counts; Clippy required a test slice cleanup.
The final short scratch directory remained inside the required task root. These
were not reasons to bypass any live gate.

PROVEN_LIVE:

- Optimized installation retained the exact designated signing requirement:
  identifier `io.github.cigit-zgy.human-in-loop` and certificate root fingerprint
  `710a013e00864b3c431d4a2167e48474829c2b08`. Both installed binaries pass strict
  signature verification, are byte-identical, and have SHA-256
  `03bb212bf94019959fa0a9a32a879741739ee7a7f8bc8a3926fc9d8bb0d2fb1b`.
- Daemon started unattended after GUI removal. Setup reports Bot session,
  Messages/iMessage, distinct identity, Full Disk Access and Automation READY,
  with Setup COMPLETE. No password, user switch or TCC prompt was needed.
- The real client launched exactly the Codex-registered binary with `mcp`, used
  canonical HOME, initialized and verified the two-tool surface, and issued
  exactly one ask call. It used a synthetic multiline card with two stable
  choices and the quoted immutable task URL; no production/scientific state.
- One canonical result returned with `received_correctly` and `imessage`.
  Bot-owned diagnostic counters independently confirmed one matching outgoing
  record and one compatible post-send inbound reply. No resend occurred.
- The client exited with code 0; daemon active requests returned to zero and
  imsg process count returned to zero. The daemon and worker remain intentionally
  installed/running; unrelated pre-existing MCP clients were not terminated.
- No GUI host or GUI login plist reappeared after headless startup and the real
  round trip. The updater cannot check/apply or display cached 0.13.x through the
  maintained production path.

INFERRED_ONLY / limits:

The fresh winner was not instrumented to distinguish live-watch from catch-up
delivery. The forced-silent deterministic scenario proves catch-up independently;
the fresh real result proves the complete deployed path. The original missing-row
timing and Apple-service behavior are not reconstructed. Link presentation was
not independently re-screenshotted; its inherited quoted wrapper was preserved
and no separate probe was sent. No reboot/autologin change was performed.

## Authority, implementation ownership and local state

ChatGPT supplied the committed task and frozen design. Codex authored the scoped
runtime repair, diagnostic, regression additions and this evidence report without
changing `design/`, scientific semantics, or the public two-tool MCP contract.

Authorities actually used:

- `cigit-zgy/agent-collaboration@8601466216515125bf8b17893b2a8e8673bab79e`.
- Machine-pinned `cigit-zgy/human-in-loop@40381285c68d67f6e23ba4e9d637855923df2d4f:SKILL.md`.
- Shared Ponytail `full` from
  `DietrichGebert/ponytail@2ed6c52c9d7e5e56942508591085fd45dea277d3:skills/ponytail/SKILL.md`,
  read at its exact GitHub revision. Local discovery was not revision proof.

Task state stays under
`/Users/wenv/Documents/skills/human-in-loop/tmp/HUMAN_IN_LOOP_HEADLESS_INBOUND_SYNC_20/`.
`live-result.json` contains only canonical/redacted result and cleanup evidence;
`n/result.json` contains the final synthetic verifier summary. Test logs,
optimized build, rollback binaries/plist and worktree remain for acceptance and
the explicitly authorized post-acceptance release. Existing dependency material
was reused; no dependency/environment reinstall was performed. No live raw
Messages output was saved. Other worktrees and primary master remain untouched.

## Git delivery and notification

Resumption started clean at `e939083a2409b2c8a6ccd194e620f14241135d30` after a fresh
fetch. Source implementation was committed and pushed; a fresh fetch proved:

```text
HEAD = tracked upstream = FETCH_HEAD
     = 7dd8782823037ed944d9bfd031b85493695c47bd
```

This report is then committed separately on the same task branch and pushed,
followed by another HEAD/upstream/FETCH_HEAD equality check. The final response
identifies that actual report-containing commit; no self-referential commit hash
is fabricated inside the report.

The prior execution's BLOCKED notification remains historical. This resumed
execution uses zero progress notifications and at most one terminal notify_human
attempt, only after report publication and fresh-fetch equality, with no retry
or acknowledgement wait. Its actual delivery outcome is terminal tool evidence.

No master merge, version bump, tag or release was performed. The binary retains
the inherited package version until the separate v0.1.3 release step. After
ChatGPT acceptance PASS, the standing User authorization permits the routine
release workflow directly, subject to its actual gates.
