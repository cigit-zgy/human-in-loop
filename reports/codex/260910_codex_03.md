---
artifact_type: codex_report
task_id: HUMAN_IN_LOOP_IMESSAGE_ONLY_FINAL_19
title: iMessage-only product consolidation and live qualification
verdict: BLOCKED
date: 2026-09-10
repository: cigit-zgy/human-in-loop
task_branch: task/260910-imessage-only-v0.1.3
task_source_sha: 572a6a9a5a8a6512f223d443ad14ae3b6b204a64
baseline_sha: 40d59d2d129f308a6a2c839918faf4259ea067f6
verification_level: level_3
summary: >
  Removed Feishu from the maintained product, qualified deterministic and
  installed startup gates, and visually qualified the quoted report locator on
  one real iPhone message. The final live decision gate is blocked because the
  delivered phone reply never appeared as a new Bot Messages row, so strict
  correlation correctly returned no canonical result.
limitations:
  - The one real MCP request produced no canonical selected_choice_id or source_channel_id.
  - Apple reported the phone reply delivered, but the dedicated Bot Messages database did not sync a new row during the bounded wait.
  - The live request was cancelled and cleaned without retry; this task therefore cannot claim a complete round trip.
---

# iMessage-only product consolidation and live qualification — Codex report

## Result summary

| Required field | Result |
| --- | --- |
| `VERDICT` | `BLOCKED` |
| `TASK_SOURCE` | `reports/chatgpt/260910_chatgpt_04.md@572a6a9a5a8a6512f223d443ad14ae3b6b204a64` |
| `INHERITED_IMPLEMENTATION` | `4c057f816e2f52ab5ce0081eb0d72d401ce93fe1` plus inherited report head `40d59d2d129f308a6a2c839918faf4259ea067f6`; retained decimal token, rich body, default-zero progress, at-most-one terminal notification, simplified onboarding, optional Bot automatic login, TCC/Bot/no-SMS, and canonical-HOME behavior. |
| `ACTIVE_PRODUCT_FEISHU_SURFACE` | `0`; retirement policy/test assertions and historical `reports/` remain audit evidence, not runtime capability. |
| `MAINTAINED_REMOTE_CHANNELS` | `[imessage]` |
| `FEISHU_SOURCE_REMOVED` | `YES`; runtime adapter and complete provider module tree deleted, exports/imports removed. |
| `FEISHU_CONFIG_REMOVED` | `YES`; current serialization contains only iMessage plus generic coordination flags. |
| `FEISHU_KEYCHAIN_READ_REMOVED` | `YES`; the secret resolver module and `keyring` dependency are absent. No existing credential was read or deleted. |
| `LEGACY_CONFIG_INERT` | `PASS`; a legacy-shaped unknown channel object deserializes harmlessly, does not enable a channel, and is absent on reserialization. |
| `FEISHU_UI_DOCS_REMOVED` | `PASS`; settings, localization, commands, package metadata, README, current release notes, and active fixtures contain no retired surface. |
| `FEISHU_ONLY_DEPENDENCIES_REMOVED` | `PASS`; `keyring` and `prost` have zero Cargo dependency nodes. |
| `DAEMON_STARTUP` | `PASS`; the installed candidate started unattended and remained healthy with zero active requests after cleanup. |
| `SECURITYAGENT_FEISHU_PROMPT` | `ABSENT`; no SecurityAgent process appeared during stable-sign install, daemon startup, iMessage health, setup status, or MCP initialize/list. |
| `MCP_SURFACE` | Installed stdio initialize PASS; tools/list exactly `ask_human`, `notify_human`. |
| `IMESSAGE_HEALTH` | `ready`; dedicated worker `ready`; Automation `automation_ready`. |
| `MACOS_SETUP` | `Setup COMPLETE` with all required checks READY/VERIFIED. |
| `NUMERIC_TOKEN_FORMAT` | Real phone rendered one normal five-digit ASCII-decimal token with no leading zero. No token value is retained in this report. |
| `REPLY_GRAMMAR` | Phone rendered exact `<TOKEN>-<OPTION_NUMBER>` instructions; User sent that exact grammar and Apple marked it Delivered. |
| `CORRELATION_REGRESSION` | Deterministic PASS for chat, post-send row, GUID/reply-to, token, choice, expiry, direction, reaction, attachment, terminal, duplicate and cancellation predicates. Live strict correlation produced no result because no new Bot row arrived. |
| `RICH_BODY_REGRESSION` | PASS through production MCP synthetic verifier with a 600–800-character synthetic multiline body and canonical stable-choice result. |
| `NO_SMS_REGRESSION` | PASS; the maintained builder requires explicit `--service imessage --no-sms-fallback`; no reachable carrier fallback was added. |
| `AUTOLOGIN_REGRESSION` | PASS in the complete Node/static suite; no login, FileVault, SIP, TCC or account setting was changed. |
| `HOME_MIGRATION_REGRESSION` | PASS in the full suite; installed MCP inherited no `HUMAN_IN_LOOP_HOME` override and used the canonical configuration. |
| `PROGRESS_DEFAULT` | `0`, unchanged. |
| `TERMINAL_NOTIFY_MAX_PER_TASK` | At most `1`, unchanged. |
| `LINK_WRAPPER_FINAL` | Frozen as `Report: "<unchanged HTTPS URL>"` in design 08/10 and tests. |
| `RICH_LINK_PREVIEW_VISUAL_RESULT` | `PASS`; User observed no large Apple Rich Link Preview on the single real phone message. |
| `LOCATOR_TAPPABLE_RESULT` | `PASS`; User actually opened the locator before selecting the corresponding choice. |
| `LOCATOR_COPYABLE_RESULT` | `PASS`; the full locator remained intact/readable, and the User supplied a complete transcription. |
| `REAL_ASK_HUMAN_RESULT` | `BLOCKED_AFTER_DELIVERY`; request sent and phone reply Delivered, but no correlated Bot row/MCP terminal response appeared during a bounded wait longer than fifteen minutes. |
| `REAL_SELECTED_CHOICE_ID` | `UNAVAILABLE`; not inferred from chat text. |
| `REAL_SOURCE_CHANNEL_ID` | `UNAVAILABLE`; no canonical MCP result existed. |
| `REAL_SEND_COUNT` | `1`; one installed-MCP tools/call, one observed application message, no retry and no fallback probe. |
| `REAL_RESULT_COUNT` | `0`; strict fail-closed behavior preserved. |
| `CLEANUP` | `PASS`; cancellation reduced active requests to zero, reaped the only watcher, terminated the task-owned MCP client, and left no task-owned live client/process. The managed production daemon remains intentionally running and idle. |
| `TEST_SUMMARY` | Rust `1225 passed / 0 failed / 3 ignored`; Vitest `178 passed`; Node/static `32 passed`; production MCP verifier `20/20`; fmt, clippy, frontend build, optimized Tauri build, Skill validation, and diff checks PASS. |
| `SECURITY_SUMMARY` | Semgrep security-audit: `107` rules over `381` tracked targets, `0 findings`; two parser warnings were manually classified as Semgrep TypeScript parser limitations in unchanged valid syntax. `pnpm audit --prod`: no known vulnerabilities. |
| `PRIVACY_SUMMARY` | No private recipient, sender identity, chat id/GUID, row id, message token, credential, Keychain content, or raw private Messages content is in Git or this report. The direct Bot database remained unreadable to the primary user and was not bypassed. |
| `WORKTREE_CLEANUP` | Task-owned live request/client/watcher cleanup PASS. Disposable synthetic and scan scratch is removed after durable reporting; the linked worktree is removed after final push/fetch proof. |
| `FRESH_FETCH_PROOF` | Implementation/design head `18eacd15354827842b7d441873016a0a1312f1a2` equalled local HEAD, tracked upstream and fresh FETCH_HEAD before report authoring. The final report commit is independently fresh-fetched after push and supplied in the terminal response. |
| `HUMAN_NOTIFICATION` | At most one terminal `notify_human` attempt occurs only after this report is committed, pushed and fresh-fetched; notification outcome does not rewrite `BLOCKED`. |

## Implementation summary

ChatGPT consolidated the single-channel decision into the task commit. Codex
mechanically moved those semantics into the normal owning design topics and
deleted the transitional topic in commit
`f34b944a2799a7c8f8c97673145f105a39b60f8b`.

Production commit `eb2f330c3ba875e6e86027d2ca83987f30f7a205`
removed the provider-specific runtime, channel adapter, card/client/router/token
and WebSocket code, config and secret resolver, Keychain dependency, protobuf
dependency, dispatch/racing branches, enum variants, daemon availability,
commands, UI/settings/localization, current documentation and fixtures. The
existing iMessage request, renderer, worker, send/watch and strict correlation
path remains the sole maintained remote path.

The removal is deliberately tolerant of old config JSON. Serde ignores unknown
legacy objects, while current serialization never recreates them. No migration
enumerates, reads or deletes old OS Keychain items.

The real phone visually qualified the already-designed quoted locator. Commit
`18eacd15354827842b7d441873016a0a1312f1a2` freezes that observed presentation
in the two owning design topics. No alternative wrapper, private API, malformed
URL or second message was used.

## AI-assisted implementation transparency

Codex performed the removal and tests under the committed ChatGPT task and
living design. New tests first failed against the two-channel surface, then
passed after the smallest product removal. The production implementation does
not add a replacement transport, credential store, fallback, coordinator,
dependency or public capability.

Legacy AskHuman-era Telegram, Slack and DingTalk implementation remnants remain
outside the maintained remote product exactly as permitted by task scope. They
were not promoted to config/UI/MCP fallback and were not broadly rewritten.

## Shared coding-Skill alignment

- Collaboration authority: `cigit-zgy/agent-collaboration@8601466216515125bf8b17893b2a8e8673bab79e` — `REMOTE_PIN_USED`.
- Shared coding profile: `Ponytail/full` pinned at `2ed6c52...` — verified through the project Skill inventory before implementation.
- Human checkpoint/terminal policy: task/current design plus the machine-wide pinned human-in-loop Skill.
- Activated workflow Skills: `agent-collaboration`, `human-in-loop`, `task-progress`, `skill-creator`, `Ponytail/full`, `systematic-debugging`, `test-driven-development`, `executing-plans`, `using-git-worktrees`, and `verification-before-completion`.

## Verification evidence

### Component, contract and repository regression

- TDD red phase proved settings/CLI, desktop command registry, confirmation
  registry and compiled/frontend source still exposed the retired provider.
- Final static boundary checks settings, CLI, command registry, confirmation
  registry, all Rust/TypeScript/Vue source, current docs and test fixtures.
- Full Rust: `1228` discovered, `1225` passed, `0` failed, `3` ignored.
  Ignored tests are the explicit real same-account round trip, manual DOCX
  sample emitter and environment-dependent real Codex fork-help probe. This
  task's same-account live action was run explicitly, not hidden as an
  ignored test.
- `cargo fmt --check` and `cargo clippy --all-targets -- -D warnings`: PASS.
- Vitest: `178/178`; Node/static: `32/32`; `pnpm build`: typecheck and production
  Vite build PASS. The existing large-chunk advisory is non-blocking and
  unrelated to this task.
- Skill validator: PASS; `git diff --check`: PASS.

### MCP protocol, integration, cancellation and race evidence

The optimized production executable passed `scripts/mcp-verify.mjs` through a
real stdio MCP client, production daemon/coordinator/renderer and finite
synthetic iMessage boundary:

- initialize, negotiation, closed schemas and exactly two tools;
- invalid/oversized/private payload rejection before send;
- Unicode, 2/6 choice limits, recommendation and canonical result mapping;
- WME-sized synthetic multiline detail;
- strict row/GUID/token/chat/choice/reaction/attachment/outgoing rejection;
- uncertain send with no retry, EOF failure and recovery;
- explicit cancellation during waiting/preparation/mutation;
- concurrent token and request isolation, first-terminal-wins;
- notification terminal states/failure/cancellation;
- graceful drain, forced stop, EOF and repeated process cleanup.

Result: `20` categories PASS, `30` synthetic application sends, `0` real sends.
The first long-path attempt failed only because the scratch socket exceeded
macOS `SUN_LEN`; the task-authorized short alias under repository `tmp/` passed
without product changes.

### Production build, install and startup

- `pnpm tauri build --no-bundle`: optimized release binary produced.
- Stable routine install completed without sudo, password or native prompt.
- Install state records the exact unsigned source hash and stable-signed
  installed hash; strict codesign and designated requirement verification pass.
- Installed binary contains no retired provider/domain marker.
- Installed daemon started unattended, iMessage channel health is `ready`, Bot
  worker and Automation are ready, setup is COMPLETE, and SecurityAgent remained
  absent.
- Installed stdio MCP initialize/list returned exactly `ask_human` and
  `notify_human`; canonical Codex registration contains no managed isolated HOME
  override.
- The single phone send preceded removal of one unrelated stale frontend
  explanatory string. The final source was then rebuilt, stable-signed,
  installed and re-smoked without another real send. The iMessage production
  path was unchanged, but this report does not claim an exact-final-binary live
  canonical result; no such result exists in this run.

### Security, dependency and privacy evidence

- Semgrep found zero security findings. Its two warnings were partial-parser
  limitations at valid TypeScript generic/dynamic-import type syntax; frontend
  typecheck and Vitest independently passed those files.
- `pnpm audit --prod` found no known vulnerability. `cargo-audit` was not
  installed; Cargo tree inspection independently proved `keyring` and `prost`
  absent, and the full lockfile supply-chain policy ran as part of `pnpm test`.
- The maintained send builder still requires explicit iMessage and no-SMS flags.
- Generic command/file/credential/raw database capability was not added to MCP.
- The task never read or deleted a historical credential, never reset TCC,
  never changed Apple Account, recipient, FileVault/SIP or Bot topology, and
  never touched WME/scientific state.

## Live qualification evidence

### Proven live

One genuine installed-MCP request used synthetic, non-production content. The
phone displayed one application message with the repository label, a normal
five-digit decimal token, exact numeric-hyphen reply instruction, four stable
semantic choices and the quoted immutable task locator. The User observed no
large preview, opened the URL successfully, supplied a complete transcription,
sent the exact numeric-hyphen answer, and confirmed Apple displayed Delivered.

There was exactly one application send. No transport retry, visual fallback,
second link message, progress notification or real scientific mutation occurred.

### Blocked live

The installed MCP call remained pending for longer than fifteen minutes after
the User's Delivered reply. The same original request and watcher stayed active;
no new Bot Messages row reached the watcher, so no correlation predicate was
weakened and no canonical result was manufactured. Read-only health continued
to report worker ready, Automation ready, iMessage ready and Setup COMPLETE.

Direct access to the Bot Messages database was denied to the primary user as
designed. Codex did not bypass that boundary with sudo, user switching, private
API access or raw message inspection. The precise unresolved boundary is:

```text
iPhone exact reply marked Delivered
→ dedicated Bot Messages database did not surface a new row
→ watcher could not evaluate strict correlation
→ MCP result remained absent
```

The live client was then cancelled. Daemon active requests returned to zero,
the watcher was reaped, and no task-owned client remained. Because the committed
acceptance requires a real canonical `selected_choice_id` and
`source_channel_id=imessage`, this evidence mandates `BLOCKED` rather than PASS.

## Evidence classification

### PROVEN_DETERMINISTIC

- iMessage-only current source/config/UI/docs/dependency surface;
- legacy config inertness and no Keychain secret path;
- public MCP tool/schema/result boundary;
- decimal token and exact reply parser;
- rich body, no-SMS, canonical-HOME, terminal notification and autologin regressions;
- cancellation/race/exactly-once logic and process cleanup;
- complete Rust/frontend/static/build/security checks.

### PROVEN_LIVE

- stable-signed candidate install and unattended daemon startup;
- no SecurityAgent prompt;
- iMessage/worker/Automation readiness and Setup COMPLETE;
- installed MCP initialize/list exactly two tools;
- exactly one real application message;
- decimal token and reply instruction rendered;
- quoted locator has no large preview, is tappable and remains complete;
- User's exact reply was Apple-Delivered;
- cancellation cleanup returned to zero active requests/watchers.

### PLATFORM_LIMITATIONS

- Apple delivery acknowledgement did not imply that the dedicated Bot Mac had
  synchronized the inbound row during this run.
- The authorized task forbids switching users, changing Apple Account, resetting
  TCC or rebooting, so deeper Bot Messages sync recovery is outside this task.

### INFERRED_ONLY

- None of the missing canonical result fields is inferred. The User-selected
  visual choice is evidence for presentation only and is not substituted for an
  MCP semantic result.

## Final judgment

The implementation and every deterministic/installed pre-send gate are ready
for acceptance review. The former disabled-provider Keychain blocker is gone,
and the first real message fully qualifies the desired link presentation.

Release qualification remains blocked at a distinct external/runtime boundary:
the dedicated Bot Messages account did not synchronize the delivered inbound
reply into a watcher-visible row. A follow-up recovery task should diagnose that
account/session sync state without resending until it can prove whether the
existing delivered reply is recoverable. It must not weaken correlation or treat
the User's local transcription as the canonical MCP result.
