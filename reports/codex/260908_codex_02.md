---
artifact_type: codex_report
task_id: HUMAN_IN_LOOP_LOCAL_CLEANUP_BOT_BOOTSTRAP_07
title: Clean local runtime and pause for Bot bootstrap
verdict: BLOCKED
date: 2026-09-08
repository: cigit-zgy/human-in-loop
task_branch: task/260908-local-cleanup-bot-bootstrap
task_source_sha: c1455a25e8e5ff11a71a8e77efb62e3b84a05265
baseline_sha: 22923f849b1fa86a70858c2d13e60f6210d35cec
verification_level: level_2
summary: >
  Replaced the active macOS executable, daemon, MCP, install, and application
  identity with human-in-loop; removed the independent local AskHuman runtime
  surface after replacement smoke; and stopped before any real message because
  the dedicated Bot macOS user and Messages login do not yet exist.
limitations:
  - The dedicated human-in-loop macOS user has not been created or logged in.
  - The Bot Apple Account is therefore not signed into Messages.app and no real iMessage qualification was permitted.
  - One large prior task scratch directory is quarantined under this task's recovery directory pending safe final cleanup.
---

# Clean local runtime and pause for Bot bootstrap — Codex report

## Changes

- Renamed the macOS production binary/default run, Tauri product identity, installer target/state, daemon label, runtime messages, primary home variable, default home, and installed MCP command to `human-in-loop`.
- Corrected the installed CLI and generated MCP guidance to identify the `human-in-loop` server and its maintained `ask_human` plus `notify_human` surface.
- Added `scripts/runtime-identity.node-test.mjs` to hold the executable, install, daemon, home, MCP, documentation, and prompt identity contract.
- Made Vitest workers disable Node 26's experimental global Web Storage when supported so jsdom owns the test-local storage implementation; no production browser behavior changed.
- Installed the release binary at `~/.local/bin/human-in-loop`, then removed the independent global npm package, old command shims/local binaries, old launchd registrations, old processes, and old runtime config/cache from their active locations. Deletions were moved to Trash where practical so they remain recoverable.
- Updated the existing Codex MCP registration in place to launch the project-owned binary and primary `HUMAN_IN_LOOP_HOME`; unrelated Codex configuration was preserved.
- Preserved required upstream license/provenance material. No standalone upstream source checkout or installed `.app` bundle existed.
- Disabled the existing private same-account iMessage channel configuration without printing or rewriting its recipient. No message send was attempted.

The task artifact was committed before local execution. The baseline already contained the canonical coordinator, MCP tools, channels, and headless daemon. Codex authored the task-scoped runtime identity changes, regression test, local cleanup, install, and verification described here.

## AI-assisted implementation transparency

Codex made the smallest local-production rebrand needed to remove the old executable/app/MCP/daemon identity while retaining upstream attribution and compatibility-only internal state names that are not active product identity. The control flow remains MCP/CLI → existing daemon/coordinator → configured channel; no second runtime, coordinator, transport, dependency, or IPC protocol was introduced.

The Node 26 test repair is conditional on the runtime advertising the supported disable flag. The first attempted spelling was rejected by the focused test, then corrected to the canonical `--no-experimental-webstorage` flag and verified by the full suite.

## Shared coding-Skill alignment

- Collaboration authority: `cigit-zgy/agent-collaboration@8601466216515125bf8b17893b2a8e8673bab79e` — `REMOTE_PIN_USED`; the local checkout was at a different revision and did not override the task pin.
- Shared coding profile: `DietrichGebert/ponytail@2ed6c52c9d7e5e56942508591085fd45dea277d3:skills/ponytail/SKILL.md`, mode `full` — aligned to the immutable profile and applied as the minimalism constraint.
- Project Skill: `human-in-loop` — used for the explicit human checkpoint and terminal-report boundary; it did not override the committed task.
- Local helper Skills: test-driven development, systematic debugging, and verification-before-completion. They supplied execution discipline only and did not redefine shared or project authority.

## Verification evidence

### Runtime identity contract

- The new Node contract was observed failing before the rebrand and passing afterward: `1 passed, 0 failed`.
- Final installed help identifies `human-in-loop`; MCP help lists `ask_human` and `notify_human`.
- Installed version: `human-in-loop v0.13.1`.
- Final ad-hoc signature identifier: `io.github.cigit-zgy.human-in-loop`; no Team ID was claimed.

### Rust, lint, and frontend

- `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check`: PASS.
- `cargo test --manifest-path src-tauri/Cargo.toml --quiet`: `1228 passed, 0 failed, 3 ignored` from 1231 tests.
- The three existing ignored tests are the deliberately real same-account iMessage round trip, a manual DingTalk DOCX sample emitter, and an environment-dependent real Codex fork-help check. The iMessage test was intentionally not run because this task forbids a real send before the Bot checkpoint.
- `cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings`: PASS. Cargo emitted the existing future-incompatibility notice for transitive dependency `block v0.1.6`.
- `pnpm test`: 28 Vitest files / 178 tests passed, followed by 10 Node/static tests passed; 0 failed.
- `pnpm build`: Vue typecheck and production Vite build passed; 2317 modules transformed. The existing large-chunk advisory remains non-fatal.

### Production install and headless smoke

- `./scripts/install.sh --global --release` rebuilt the optimized final candidate, installed the project-owned binary, and replaced its signature successfully.
- With a task-isolated runtime home, `daemon start` reported version 0.13.1, protocol 2, zero active requests, and no IM connections; `daemon status` agreed; `daemon stop` exited cleanly.
- No GUI `.app` was needed for build, install, daemon, MCP initialization, schema inspection, or shutdown. No AskHuman or human-in-loop `.app` was installed. Result: `GUI App NOT_REQUIRED` for this production path.

### Installed MCP protocol smoke

An actual stdio MCP client launched the installed release binary, completed initialize at protocol `2025-11-25`, observed server name `human-in-loop`, and listed exactly `ask_human` plus `notify_human`. The `ask_human` schema exposed the 2–6 choice boundary. A deterministic one-choice call was rejected before coordinator/channel dispatch, and EOF produced exit code 0. No message-producing call was made.

The broader synthetic helper was also attempted from a nested task path and failed before daemon readiness because the derived Unix-domain socket exceeded macOS `SUN_LEN`. Direct reproduction confirmed the path-length cause. It was not counted as a product PASS; the required installed protocol and daemon smoke were rerun successfully from the task root, whose socket path fits the OS limit.

### Local cleanup and privacy

Final active-surface audit returned zero old commands/symlinks, zero exact-name old processes, zero old launchd entries, zero old MCP registrations, zero installed apps, zero old active runtime/config/cache paths, and zero standalone upstream clones. The installed project binary was executable. A second cleanup check found zero project-owned smoke processes and zero launchd entries after shutdown.

The active private runtime configuration was inspected structurally only: iMessage is disabled, identity mode remains `same_account`, Feishu is disabled, and the private recipient value was neither printed nor copied into Git evidence. Diff scans found zero local user paths, private identity strings, private mail-domain strings, or credential assignments. No Apple credential, Messages content, token, chat identifier, or screenshot was collected.

### Bot preflight and message gate

`dscl` confirmed the standard macOS account `human-in-loop` is absent. Consequently Bot session status, Messages Apple Account state, sender identity, TCC permissions, direct chat, and cross-user worker readiness cannot yet be validated. The previous same-account iMessage configuration is disabled, so the current runtime fails closed. Exactly zero real iMessage mutations occurred.

## Local temporary state

Temporary workspace: `<PROJECT_ROOT>/tmp/HUMAN_IN_LOOP_LOCAL_CLEANUP_BOT_BOOTSTRAP_07/`

Final state: RETAINED

Reason: the linked worktree is the clean, pushed continuation point for this blocked FORMAL task. A 9.8GB prior same-account scratch tree had no active process or registration; moving it to Trash blocked for several minutes without completing, so the untouched source was atomically quarantined at `recovery/HUMAN_IN_LOOP_SAME_ACCOUNT_02/`. It is non-active recovery material and will be reassessed after the User checkpoint. All smaller no-recovery-value smoke state was moved to Trash. A separate prior scratch tree with documented acceptance/recovery value was preserved outside this task and was not treated as disposable User state.

## Acceptance criteria / blockers

| Criterion | Result | Evidence |
|---|---|---|
| Independent AskHuman local runtime removed | PASS | Active commands, processes, launchd entries, MCP registrations, apps, configs/caches, and standalone clones all audited at zero. |
| `human-in-loop` is the only active runtime identity | PASS | Final release install, version, help, daemon label/output, MCP server name, and Codex MCP registration use the project identity. |
| GUI necessity decided | PASS | Headless install, daemon, configuration, and MCP smoke ran without any `.app`; GUI App is not required. |
| Replacement verified before cleanup | PASS | Release binary, daemon, and MCP smoke completed before/after old-runtime cleanup. |
| Bot macOS user/session available | BLOCKED | The `human-in-loop` account does not exist. |
| Bot Apple Account active in Messages | BLOCKED | Cannot be performed until the User creates/logs into the Bot account normally. |
| Cross-user worker and real iMessage qualification | NOT RUN | Correctly gated behind the required User checkpoint; zero real messages sent. |

USER_CHECKPOINT:
log in to macOS user human-in-loop and sign Messages.app into the dedicated Bot Apple Account.

Before that login, the User must create a standard macOS user in System Settings with both Full Name and Account Name set to `human-in-loop`. Codex must not request the password or 2FA. After creating the account, log into it, open Messages.app, sign in to the already-owned dedicated Bot Apple Account, confirm iMessage activation and the Bot sender email, fast-user-switch back to the primary account, and say `continue`. Do not change the primary user's personal Messages account.

This is an external/manual prerequisite, not an implementation failure. The committed task remains incomplete and therefore receives `BLOCKED` rather than `PASS`.

## Git result

- Initial linked worktree: task branch at task-source commit `c1455a25e8e5ff11a71a8e77efb62e3b84a05265`, tracking the equal remote branch, with no pre-existing User changes.
- Implementation commit: `2fe93f576c01cf7bc4a58e6c1d4a6bf63b984115` (`feat: adopt human-in-loop runtime identity`).
- The implementation commit was pushed and fresh-fetched; local HEAD, tracked upstream, and `FETCH_HEAD` were equal before report authoring.
- The report-containing commit is the final task-branch handoff identified by the immutable link in the terminal response. The primary checkout, default branch, repository visibility, tags, and releases were not changed.
