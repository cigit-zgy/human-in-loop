---
artifact_type: codex_report
artifact_id: 260907_codex_01
task_id: HUMAN_IN_LOOP_BOOTSTRAP_01
title: Import AskHuman baseline and implement Feishu plus iMessage-only channels
date: 2026-09-07
project: human-in-loop
repository: cigit-zgy/human-in-loop
status: completed
summary: >
  Imported the pinned AskHuman application, retained Feishu, added a bounded
  external-imsg iMessage adapter, removed legacy channel reachability, completed
  automated verification, and resumed real E2E through the macOS permission gate.
verdict: BLOCKED
task_branch: task/260907-imessage-feishu-bootstrap
task_source_sha: c62d23d688bf67e630cb2c0a86f96eba5f55e3f5
baseline_sha: 77f284173522768b6e594e2201077f288407c69d
verification_level: level_3
limitations:
  - Real macOS iMessage E2E is blocked because the approved private recipient has no resolvable existing direct conversation or known iMessage service in the local Messages database; all send/watch/correlation steps remain unattempted.
---

# Changes

- Imported application source from `Naituw/AskHuman@77e2e576347f94ef203bc2426b73a18749cb4e92` without replacing `AGENTS.md`, living design, concept history, task authority, project license, or project README semantics. The imported MIT notice is preserved byte-for-byte at `LICENSES/AskHuman-MIT.txt` (SHA-256 `8d97ef3970765bdf461a0cf4d68342bc40df0b3b50bbeca87190cdd4189798d0`). No nested repository or gitlink is retained.
- Limited maintained channel surfaces and routing to Feishu and Apple Messages. Persisted channel configuration, settings, search, CLI, npm-facing documentation, runtime candidate registries, health output, and desktop command registration do not expose Telegram, Slack, DingTalk, or popup delivery. Imported low-level legacy modules remain internal compilation units but have no product/config/registry/invoke reachability.
- Preserved the shared AskHuman canonical confirmation/coordinator and Feishu long-connection/card path. Added a single optional canonical `DecisionImage` field rather than a transport-specific request model.
- Added `src-tauri/src/channels/imessage.rs` around the external `openclaw/imsg@646ea7af9616dc3e6406d86aa269bf4fb1b07a76` contract. The adapter verifies one stored direct iMessage conversation, starts a chat-scoped NDJSON watcher, sends once, classifies health failures, and kills/reaps the watcher on every terminal path.
- Enforced the structured budgets, exact uppercase-token reply grammar, active-token collision handling, stable wire-choice mapping, wrong/self/stale/late/reaction/attachment rejection, and PNG/JPEG maximum-one-image admission at 5 MiB.
- Constructed every Apple Messages mutation with `--service imessage --no-sms-fallback`; no `auto`, SMS, MMS, RCS, carrier relay, private IMCore, or SIP-disabled path was added.

## Continuation after the original blocker

- The first execution ended at report commit `25fb5202b5c5733cafe57897f6919b4dda527ab0` because `imsg` was absent. On 2026-09-07 the User explicitly authorized installation/configuration of the pinned-compatible dependency and approved one exact private runtime recipient. The recipient is intentionally omitted from repository evidence.
- Codex built only the public `imsg` executable from `openclaw/imsg@646ea7af9616dc3e6406d86aa269bf4fb1b07a76`, whose version file declares `0.15.1`. No bridge helper, injection component, Advanced IMCore setup, or SIP change was built or installed.
- The ad-hoc signed arm64 binary and its two required public SwiftPM resource bundles were installed under `/Users/wenv/.local/bin/`. `command -v imsg` resolved that path, `imsg --version` returned `0.15.1`, code-sign verification passed, and the installed binary SHA-256 was `b4a41f40840ca961aa1c03a4e3f7bd12fdb5a6926078d465d914d32eed391623`. A post-cleanup `imsg chats` smoke confirmed the installed artifact no longer depended on task scratch; no bridge dylib was installed.
- The first database-read permission probe redirected all message output to private task scratch and failed with `authorization denied (code: 23)`, identifying missing Full Disk Access for the active ChatGPT.app → Codex process chain. System Settings was opened to Privacy & Security → Full Disk Access. After the User granted access, the same probe succeeded with no stderr, resolving that checkpoint.
- Peer resolution then scanned 2025 local chat records without exposing their contents. No participant or identifier matched the approved private recipient, and SIP-free `imsg whois --local` reported the handle as unknown rather than iMessage. Consequently no deterministic direct chat id/GUID exists for project configuration, and self/distinct-peer topology cannot yet be classified.
- In accordance with the explicit fail-closed rule, no private project configuration was written. The task-built `AskHuman channel test imessage` was run with an isolated empty configuration and returned `Error: not_configured` with exit 1, which is the expected fail-closed project health state while no verified chat id/GUID exists. Execution stopped before send, watcher, or correlation; no message was sent and no carrier path was attempted.

# AI-assisted implementation transparency

ChatGPT authored the committed design authority and formal task. Codex performed the local upstream import, implementation, tests, build/install execution, audit, and this report. No ChatGPT-authored implementation patch was supplied. The final implementation remains subject to independent ChatGPT acceptance; this Codex report is not acceptance.

# Shared coding-Skill alignment

- Resolved and followed `cigit-zgy/agent-collaboration@8601466216515125bf8b17893b2a8e8673bab79e`, including its formal execution, verification, report, project-report, and shared-coding-Skill contracts.
- Applied the pinned Ponytail `full` profile. The locally discovered Skill content matched the pinned source file by SHA-256 (`1316a2f3f95741d2300b116fe0c2d81ce4a9568656ed0a62643f54aaf09957f2`).
- Used test-first red/green checks for the new legacy-surface and desktop-command-registry gates, then ran the complete Level 3 verification.

# Verification evidence

| Gate | Result | Evidence |
|---|---|---|
| Import identity and license | PASS | AskHuman checkout resolved to `77e2e576347f94ef203bc2426b73a18749cb4e92`; the copied license hash matched `git show HEAD:LICENSE`; imported LFS assets were materialized and no pointer text remained. |
| Rust unit suite | PASS | `cargo test --manifest-path src-tauri/Cargo.toml --quiet`: 1185 tests, 1183 passed, 0 failed, 2 ignored. |
| Rust format/lint | PASS | `cargo fmt --check`; `cargo clippy --all-targets -- -D warnings`. The only emitted note was a future-incompatibility notice for imported transitive `block v0.1.6`. |
| Frontend and static suite | PASS | `pnpm test`: 28 Vitest files / 178 tests passed; 9 Node tests passed, including channel-surface, desktop registry, explicit iMessage send, and platform icon gates. |
| Frontend production build | PASS | `pnpm build`: `vue-tsc --noEmit` and Vite build completed (2317 modules). Vite emitted only the existing large-chunk advisory. |
| Tauri build | PASS | `pnpm tauri build --no-bundle` produced the release `AskHuman` binary in the task-scoped Cargo target. |
| Install | PASS | `scripts/install.sh` completed the frontend build, local-install Rust profile, task-scoped installation, ad-hoc code signing, and signature verification. |
| Installed artifact smoke | PASS | `AskHuman --version` returned `v0.13.1`; `channel help` named only `feishu | imessage`; isolated `channel list --json` returned exactly those two channels. |
| Missing dependency behavior | PASS | Isolated `channel test imessage` returned `Error: imsg_missing` with exit 1 and did not invoke a send mutation. |
| No-carrier/static boundary | PASS | Production `imsg` process construction is isolated to the adapter; focused source and argument-builder tests found no auto/SMS service path or fallback enablement. |
| Targeted security scan | PASS | Semgrep `p/rust`: 11 rules over the iMessage adapter, confirmation integration, and configuration; 0 findings. |
| Previous missing-dependency blocker | RESOLVED | The first run recorded `command -v imsg` as absent. The resumed run received explicit User authorization and installed the exact pinned-source CLI as described above. |
| Pinned transport identity | PASS | Installed `imsg` reports `0.15.1`; source HEAD was exactly `646ea7af9616dc3e6406d86aa269bf4fb1b07a76`; installed signature and artifact hash were verified; a post-scratch-cleanup database smoke passed with the required public resource bundles adjacent to the binary. |
| Full Disk Access | PASS | The initial `authorization denied (code: 23)` was preserved as history; after the User enabled access, `imsg chats --limit 1 --json` exited 0 with one private JSON record and no stderr. No record content was printed or committed. |
| Automation → Messages | NOT ATTEMPTED | The accepted flow reaches the legitimate Automation prompt only at the first send; sending is prohibited until database access and exact peer validation pass. |
| Peer and topology validation | BLOCKED | Across 2025 local chat records there were zero exact/case-insensitive participant matches and zero identifier matches for the approved private recipient; `imsg whois --local` returned `known=false` and did not identify iMessage. Topology remains unclassified. |
| Project-level channel health | PASS (fail-closed) | The task-built `AskHuman v0.13.1` ran under an isolated empty configuration; `channel help` exposed only Feishu and iMessage, and `channel test imessage` returned `Error: not_configured` with exit 1. No recipient, chat id, or GUID was invented or persisted. |
| Real outbound/inbound E2E | BLOCKED | Zero test messages were sent. Watch/correlation/watcher-cleanup evidence awaits an existing direct conversation whose service resolves as iMessage. |

# Local temporary state

All first-run clones, LFS recovery downloads, isolated Rust toolchain/cache, dependency tree, builds, installed smoke binary, and isolated configuration were created under `tmp/HUMAN_IN_LOOP_BOOTSTRAP_01/` and removed after verification. The resumed pinned `imsg` source/build and redacted permission probe were also confined to that task path and removed before Git handoff. The external installed CLI under `/Users/wenv/.local/bin/` is the explicitly authorized persistent dependency. No private runtime recipient or Messages content was written to tracked files.

# Acceptance criteria / blockers

Acceptance criteria 1–10 have implementation and automated evidence above: project authority was preserved; maintained surfaces expose only two channels; Feishu and shared coordinator regressions pass; renderer/reply/image/no-carrier behavior has focused coverage; `imsg` remains external; and committed fixtures/report contain no credential or private-message content.

The missing executable and Full Disk Access blockers are resolved. The current genuine checkpoint is peer provenance: the already approved private recipient must first have an existing direct conversation in Messages.app whose local database record identifies exactly one participant and service `iMessage`. The User can establish/verify that conversation manually in Messages without authorizing any carrier send; after it appears locally, Codex can resume the same task to resolve its identity, classify distinct/self topology, run project-level health, and—only after iMessage validation—send the single bounded confirmation. The verdict remains `BLOCKED`, not `PASS`.

# Git result

This report is included in the task-scoped Conventional Commit on `task/260907-imessage-feishu-bootstrap`. The immutable report commit and fresh-fetch local/upstream equality are post-commit gates and are recorded in the final task locator. `master` is not merged.
